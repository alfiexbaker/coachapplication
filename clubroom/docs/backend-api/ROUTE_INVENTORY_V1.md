# `/v1` Route Inventory (Contracts-First, Phase 1)

This is the initial endpoint inventory for the Clubroom backend. It is intentionally broader than the first coded scaffold and should be implemented sprint-by-sprint.

Status legend:

- `implemented`: route exists in `apps/api` and now uses a runtime-backed repository/persistence path
- `scaffolded`: route exists in `apps/api`
- `planned`: contract + authz + data model defined, implementation pending
- `deferred`: intentionally after phase 1 or later sprint

## Current Core Routes

| Route                                            | Method      | Status        | Notes                                                                                                                                                                                                                                                      |
| ------------------------------------------------ | ----------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/v1/health`                                     | `GET`       | `scaffolded`  | contract in `@clubroom/shared-contracts`                                                                                                                                                                                                                   |
| `/v1/ready`                                      | `GET`       | `implemented` | returns real config/database/object-storage readiness and responds `503` until blocking issues are cleared                                                                                                                                                 |
| `/v1/openapi.json`                               | `GET`       | `implemented` | generated OpenAPI 3.1 document derived from this route inventory; enabled outside production by default, production opt-in via `API_OPENAPI_ENABLED=1`                                                                                                    |
| `/v1/docs`                                       | `GET`       | `implemented` | Swagger UI shell that loads `/v1/openapi.json`; enabled outside production by default, production opt-in via `API_OPENAPI_ENABLED=1`; the JSON document remains the committed source artifact                                                              |
| `/v1/meta/version`                               | `GET`       | `scaffolded`  | dev metadata only                                                                                                                                                                                                                                          |
| `/v1/meta/seed-health`                           | `GET`       | `scaffolded`  | privileged-admin-only internal seed/runtime coverage counters for staging verification; disabled when `API_DATA_BACKEND=db`                                                                                                                                |
| `/v1/bookings`                                   | `GET`       | `scaffolded`  | visible booking list scaffold for coach, booked-by guardian, linked guardian, or athlete participant; `BookingResponse` includes nullable `recurringSeriesId` and `groupSessionId` linkage fields                                                          |
| `/v1/bookings`                                   | `POST`      | `scaffolded`  | direct booking create scaffold; app treats it as the only non-mock create path, honors idempotency replay/conflict, and client booking CRUD keeps only a runtime memory mirror without API-mode local notification overlays                               |
| `/v1/bookings/:bookingId`                        | `GET`       | `scaffolded`  | booking detail scaffold with same visibility gate as list reads; `BookingResponse` includes nullable `recurringSeriesId` and `groupSessionId` linkage fields                                                                                               |
| `/v1/bookings/:bookingId/rebook-context`         | `GET`       | `implemented` | backend-authoritative repeat-booking context; visible source booking required, coach-only actors denied, and draft context is copied from backend booking proof                                                                                            |
| `/v1/bookings/:bookingId/cancel`                 | `POST`      | `scaffolded`  | booking cancellation with actor ownership enforcement, idempotency/version checks, status-event audit, linked open invoice voiding, active payment-attempt cancellation, and paid-invoice refund hard wall                                                 |
| `/v1/bookings/:bookingId/reopen`                 | `POST`      | `scaffolded`  | booking reopen restores the last active status for upcoming cancelled bookings and restores invoices voided by booking cancellation with optional idempotency replay/conflict and expected-version checks                                                  |
| `/v1/bookings/:bookingId/confirm`                | `POST`      | `implemented` | assigned-coach booking confirmation for pending/awaiting-confirmation bookings with idempotency/version checks and status-event audit                                                                                                                       |
| `/v1/bookings/:bookingId/complete`               | `POST`      | `implemented` | coach-owned booking completion with idempotency/version checks, future-session denial, per-athlete attended/no-show effort input, attendance proof record creation, progress-visible session-note proof for attended athletes when a coach note is present, route/status-event audit, and recurring-series derived status updates |
| `/v1/booking-series`                             | `GET`       | `implemented` | backend-owned visible multi-week/recurring series list for parent, coach, linked guardian, or athlete participant                                                                                                                                          |
| `/v1/booking-series`                             | `POST`      | `implemented` | backend-owned multi-week/recurring package create; writes a recurring series plus linked bookings with slot validation, relationship authz, and idempotency replay                                                                                         |
| `/v1/booking-series/:seriesId`                   | `GET/PATCH` | `implemented` | backend-owned series detail/update; reads use linked booking visibility, but updates require booked-by user or a guardian linked to every athlete; update mutates existing future linked bookings only, validates reassigned coach availability, backend-syncs mutable linked invoices and coach ownership, writes reassignment status events, and blocks settled invoice changes |
| `/v1/booking-series/:seriesId/cancel`            | `POST`      | `implemented` | backend-owned series cancellation; requires booked-by user or a guardian linked to every athlete, cancels linked future bookings only, applies linked invoice/payment-attempt cancellation effects, records status events, checks expected version, and honors idempotency replay |
| `/v1/booking-series/:seriesId/pause`             | `POST`      | `implemented` | backend-owned series pause; requires booked-by user or a guardian linked to every athlete, changes only series lifecycle state, keeps linked bookings intact, writes no-status-change lifecycle status events, checks expected version, and honors idempotency replay |
| `/v1/booking-series/:seriesId/resume`            | `POST`      | `implemented` | backend-owned series resume; requires booked-by user or a guardian linked to every athlete, restores the derived active/partial lifecycle state from linked bookings, writes no-status-change lifecycle status events, checks expected version, and honors idempotency replay |
| `/v1/athletes/:athleteId/injuries`               | `GET/POST`  | `implemented` | repository-backed injury records; db mode persists through Prisma and seed/db-fixture mode mirrors the same contract                                                                                                                                       |
| `/v1/athletes/:athleteId/squad-memberships`      | `GET`       | `implemented` | guardian/athlete/admin-scoped active squad memberships for child context; db mode reads Prisma `SquadMembership` and seed/db-fixture mode mirrors the same contract                                                                                        |
| `/v1/injuries/:injuryId`                         | `PATCH`     | `implemented` | repository-backed injury status/notes update with the same scoped actor gates                                                                                                                                                                              |
| `/v1/athletes/:athleteId/medical`                | `GET/PATCH` | `implemented` | repository-backed medical profile with doctor, insurance, restriction, emergency-note, and SEN-note fields                                                                                                                                                 |
| `/v1/athletes/:athleteId/emergency-contacts`     | `GET/PATCH` | `implemented` | repository-backed emergency contacts with stable contact IDs, primary contact, and pickup flags                                                                                                                                                            |
| `/v1/athletes/:athleteId/consents`               | `GET/PUT`   | `implemented` | repository-backed consent records covering photo, video, social, and emergency treatment flags                                                                                                                                                             |
| `/v1/safeguarding/incidents`                     | `POST`      | `implemented` | repository-backed concern ingestion with persisted incident tracking                                                                                                                                                                                       |
| `/v1/safeguarding/incidents/:incidentId`         | `GET`       | `implemented` | repository-backed incident detail                                                                                                                                                                                                                          |
| `/v1/safeguarding/incidents/:incidentId/actions` | `POST`      | `implemented` | repository-backed incident action append + status transition                                                                                                                                                                                               |

## Identity / Sessions / Auth

| Route                               | Method      | Status        | Contract(s)                          | AuthZ                                                                              | UI anchors                                                                      |
| ----------------------------------- | ----------- | ------------- | ------------------------------------ | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `/v1/auth/login`                    | `POST`      | `implemented` | scaffold login response              | public; email/password credential check                                            | auth/login screens via `services/auth-service.ts`                               |
| `/v1/auth/register`                 | `POST`      | `implemented` | scaffold register response           | public; creates account plus session                                               | onboarding/auth screens via `services/auth-service.ts`                          |
| `/v1/auth/refresh`                  | `POST`      | `implemented` | scaffold token refresh response      | refresh token only                                                                 | app token refresh path via `services/auth-service.ts`, `services/api-client.ts` |
| `/v1/auth/logout`                   | `POST`      | `implemented` | none                                 | authenticated self; revokes current session                                        | settings/logout flows via `services/auth-service.ts`                            |
| `/v1/auth/revoke`                   | `POST`      | `implemented` | none                                 | authenticated self or refresh-token self revoke                                    | security/session management via `services/auth-service.ts`                      |
| `/v1/auth/me`                       | `GET/PATCH` | `implemented` | scaffold auth profile response       | authenticated self                                                                 | auth bootstrap + profile/onboarding flows via `services/auth-service.ts`        |
| `/v1/auth/forgot-password`          | `POST`      | `implemented` | password reset request               | public; creates one-use hashed `PasswordResetToken` without account enumeration, delivers reset links through the configured email delivery provider (webhook, Brevo API, or SMTP), and only echoes reset tokens in test/explicit dev mode | auth recovery screens via `services/auth-service.ts`                            |
| `/v1/auth/reset-password`           | `POST`      | `implemented` | password reset completion            | public; validates one-use reset token, updates salted `PasswordCredential`, revokes active sessions, increments token epoch, and audits success/deny paths | auth recovery screens via `services/auth-service.ts`                            |
| `/v1/auth/verify-email`             | `POST`      | `implemented` | scaffold verify-email response       | authenticated self                                                                 | auth verification flow via `services/auth-service.ts`                           |
| `/v1/auth/check-email`              | `GET`       | `implemented` | scaffold email-availability response | public                                                                             | signup/email validation via `services/auth-service.ts`                          |
| `/v1/users/search`                  | `GET`       | `implemented` | user directory search response       | authenticated; no blank directory browse; excludes minors unless self, shared family, guardian link, or assigned active booking coach; hides private adult profiles except explicit relationship or exact-email invitation-safe lookup; sensitive reads are audited without storing raw query text; block relationships still need `/v1/blocks` authority | `services/user-service.ts#searchUsers`, guardian invite recipient lookup        |
| `/v1/me`                            | `GET`       | `scaffolded`  | `MeResponse`                         | authenticated self                                                                 | settings/account screens                                                        |
| `/v1/me/sessions`                   | `GET`       | `implemented` | scaffold session-list response       | authenticated self; audited sensitive session read                                  | settings/security                                                               |
| `/v1/me/sessions/revoke-all`        | `POST`      | `implemented` | scaffold revoke-all response         | self; audits revoked and retained session ids                                       | settings/security                                                               |
| `/v1/me/sessions/:sessionId/revoke` | `POST`      | `implemented` | scaffold revoke-session response     | self; audits success and denied revoke attempts                                     | settings/security                                                               |

## Family / Athlete / Consent / Medical

| Route                                               | Method     | Status        | Contract(s)                                             | AuthZ                                                                                                      | UI anchors                                                                                      |
| --------------------------------------------------- | ---------- | ------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `/v1/families/:familyId`                            | `GET`      | `implemented` | `FamilyResponse`                                        | guardian membership                                                                                        | `app/family/index.tsx`, `services/child-service.ts`, `services/family/family-member-service.ts` |
| `/v1/me/guardian-invites`                           | `GET`      | `implemented` | `GuardianInviteListResponse`                            | authenticated invitee email                                                                                | guardian invite inbox via `services/family/family-relationship-service.ts`                      |
| `/v1/families/:familyId/guardians`                  | `POST`     | `implemented` | `CreateGuardianInviteRequest`, `GuardianInviteResponse` | family admin guardian; duplicate same-payload pending invites replay, mismatched duplicates deny and audit | `app/family/sharing.tsx`, `services/family/family-relationship-service.ts`                      |
| `/v1/guardian-invites/:inviteId/accept`             | `POST`     | `implemented` | `GuardianInviteResponse`                                | authenticated invitee email only; creates family membership and guardian-child links                       | guardian invite acceptance via `services/family/family-relationship-service.ts`                 |
| `/v1/guardian-invites/:inviteId/decline`            | `POST`     | `implemented` | `GuardianInviteResponse`                                | authenticated invitee email only; does not grant family access                                             | guardian invite decline via `services/family/family-relationship-service.ts`                    |
| `/v1/families/:familyId/guardian-invites/:inviteId` | `DELETE`   | `implemented` | none                                                    | family admin guardian; cancels pending invite and audits success/deny paths                                | `app/family/sharing.tsx`, `services/family/family-relationship-service.ts`                      |
| `/v1/families/:familyId/guardians/:guardianId`      | `DELETE`   | `implemented` | none                                                    | family admin guardian; primary guardians cannot be removed; soft-removes guardian links and audits success/deny paths | `app/family/sharing.tsx`, `services/family/family-relationship-service.ts`                      |
| `/v1/athletes`                                      | `POST`     | `implemented` | `CreateAthleteRequest`                                  | parent/guardian                                                                                            | `app/(modal)/add-child.tsx`, `services/child-service.ts`                                        |
| `/v1/athletes/:athleteId`                           | `GET`      | `implemented` | athlete profile response plus legacy `athleteId` alias  | family membership, athlete self, or verified assigned coach                                                | `services/child-service.ts`, coach special-needs/development screens                            |
| `/v1/athletes/:athleteId`                           | `PATCH`    | `implemented` | `UpdateAthleteRequest`                                  | guardian ownership                                                                                         | `app/(modal)/edit-child-profile.tsx`, `services/child-service.ts`                               |
| `/v1/athletes/:athleteId`                           | `DELETE`   | `implemented` | no body                                                 | guardian ownership; writes `deletedAt` on the athlete and linked family records, and audits the remove action | child remove flow through `hooks/use-children-hub.ts`, `services/child-service.ts`               |
| `/v1/athletes/:athleteId/squad-memberships`         | `GET`      | `implemented` | local route response                                    | linked guardian, athlete self, or privileged admin                                                         | `hooks/use-child-context.tsx`, `services/child-service.ts`                                      |
| `/v1/athletes/:athleteId/medical`                   | `GET`      | `implemented` | `MedicalRecordResponse`                                 | guardian + verified coach (scoped)                                                                         | `app/child/[id]/medical.tsx`, `services/family/family-health-service.ts`                        |
| `/v1/athletes/:athleteId/medical`                   | `PATCH`    | `implemented` | `UpdateMedicalRecordRequest`                            | guardian                                                                                                   | `app/child/[id]/medical.tsx`, `services/family/family-health-service.ts`                        |
| `/v1/athletes/:athleteId/emergency-contacts`        | `GET`      | `implemented` | `EmergencyContactsResponse`                             | guardian + gated coach                                                                                     | `app/child/[id]/emergency.tsx`, `services/family/family-health-service.ts`                      |
| `/v1/athletes/:athleteId/emergency-contacts`        | `PATCH`    | `implemented` | `UpdateEmergencyContactsRequest`                        | guardian                                                                                                   | `app/child/[id]/emergency.tsx`, `services/family/family-health-service.ts`                      |
| `/v1/athletes/:athleteId/injuries`                  | `GET/POST` | `implemented` | `InjuriesResponse`, `CreateInjuryRequest`               | athlete self / guardian / scoped coach                                                                     | `app/health/index.tsx`, `app/health/injuries.tsx`                                               |
| `/v1/injuries/:injuryId`                            | `PATCH`    | `implemented` | `UpdateInjuryRequest`                                   | athlete self / guardian / scoped coach                                                                     | `app/health/[id].tsx`                                                                           |
| `/v1/athletes/:athleteId/consents`                  | `GET`      | `implemented` | `ConsentsResponse`                                      | guardian + verified coach (scoped)                                                                         | `app/child/[id]/medical.tsx`, `services/family/family-health-service.ts`                        |
| `/v1/athletes/:athleteId/consents`                  | `PUT`      | `implemented` | `UpsertConsentsRequest`, `ConsentsResponse`             | guardian                                                                                                   | family/child consent UIs, `services/family/family-health-service.ts`                            |
| `/v1/coaches/:coachId/roster/consents`              | `GET`      | `implemented` | coach roster consent dashboard projection               | coach self or privileged admin; reads are sensitive/audited and only include athletes already visible in the coach roster projection | `app/roster/consents.tsx`, `services/consent-service.ts` |

## Coach / Clubs / Scheduling / Verification

| Route                                                   | Method             | Status        | Contract(s)                                                                                     | AuthZ                                                                                                                                                                                                 | UI anchors                                                                                                                                                                                                                                                                |
| ------------------------------------------------------- | ------------------ | ------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/v1/coaches/me/profile`                                | `GET`              | `implemented` | `CoachProfile*`                                                                                 | coach self                                                                                                                                                                                            | `app/(tabs)/coach-profile.tsx`                                                                                                                                                                                                                                            |
| `/v1/coaches/me/profile`                                | `PATCH`            | `implemented` | `CoachProfilePatch`, refreshed `CoachProfile*` bundle                                            | coach self; updates DB-backed marketplace fields plus rich self-profile metadata (website, max price, social links, experience, languages) and audits success/deny/error paths                        | `app/(tabs)/edit-profile.tsx`, `hooks/use-edit-profile.ts`, `services/coach-profile-service.ts`                                                                                                                                                                           |
| `/v1/coaches/me/travel-settings`                        | `GET/PATCH`        | `implemented` | coach marketplace travel radius and remote/in-person availability settings                       | coach self with active coach profile; reads and writes are audited; API mode persists on `CoachProfile`, not client-local storage                                                                    | `app/settings/travel-radius.tsx`, `hooks/use-travel-radius-settings.ts`, `services/coach-travel-service.ts`                                                                                                                                                               |
| `/v1/coaches/:coachId/trial-offering`                   | `GET`              | `implemented` | coach trial-session offer or null                                                                | authenticated viewer; disabled/private offer visibility is coach self or privileged admin only; reads are audited                                                                                     | `components/coach/trial-session-editor.tsx`, trial discovery cards, `services/trial-service.ts`; API mode reads backend `CoachTrialOffering` rows                                                                                                                          |
| `/v1/coaches/:coachId/trial-offering`                   | `PUT/DELETE`       | `implemented` | coach trial-session offer                                                                        | coach self with active coach profile; delete archives/disables, not hard-deletes, and writes are audited                                                                                              | `components/coach/trial-session-editor.tsx`, `services/trial-service.ts`; API mode saves/archives through `/v1`                                                                                                                                                            |
| `/v1/trial-offerings`                                   | `GET`              | `implemented` | active trial-session offer discovery index                                                       | authenticated viewer; returns active public trial offers only; discovery reads are audited                                                                                                             | marketplace/discovery trial surfaces through `services/trial-service.ts`; API mode reads backend `CoachTrialOffering` rows                                                                                                                                                  |
| `/v1/coaches/:coachId/trial-usages`                     | `GET/POST`         | `implemented` | family/parent trial usage count and usage record                                                 | coach self or privileged admin can list coach usage; linked parent/guardian can read or record only their own booking-backed usage; records are derived from backend booking proof and audited        | booking eligibility and analytics through `services/trial-service.ts`; API mode reads and records usage through `/v1` without client-local trial storage                                                                                                                   |
| `/v1/coaches/:coachId/trial-conversions`                | `GET/POST`         | `implemented` | trial-to-regular booking conversion records                                                      | coach self or privileged admin can read or record conversions; conversion requires visible trial and regular booking proof, a linked parent/guardian, prior usage proof, and audit events             | coach analytics through `services/trial-service.ts`; API mode reads and records conversions through `/v1` without client-local trial storage                                                                                                                               |
| `/v1/coaches/me/offerings`                              | `GET`              | `implemented` | `Offering*`                                                                                     | coach self                                                                                                                                                                                            | session create + booking setup                                                                                                                                                                                                                                            |
| `/v1/coaches/me/offerings`                              | `POST`             | `implemented` | offering create input -> `Offering*`                                                           | coach self with active coach profile; persists to `CoachingOffering`, returns explicit offering output, and writes create audit events                                                                 | coach self offering management through `services/coach-offering-api.ts`; backed by the same DB authority as session templates                                                                                                                                              |
| `/v1/coaches/me/offerings/:offeringId`                  | `PATCH`            | `implemented` | offering patch input -> `Offering*`                                                            | owner coach only; PATCH is scoped to authenticated coach's own active offering and writes update audit events                                                                                         | coach self offering management through `services/coach-offering-api.ts`; backed by the same DB authority as session templates                                                                                                                                              |
| `/v1/coaches/me/session-templates`                      | `GET/POST`         | `implemented` | `SessionTemplate*` projected from active `CoachingOffering`                                     | coach self; create requires an active coach profile and mutations are audited                                                                                                                         | coach session-template presets through `services/session-template-service.ts`                                                                                                                                                                                             |
| `/v1/coaches/me/session-templates/:templateId`          | `GET/PATCH/DELETE` | `implemented` | `SessionTemplate*` projected from active `CoachingOffering`                                     | coach self; item must belong to authenticated coach; update is audited and delete audits as `coach_session_template.archive`; delete is soft                                                          | coach session-template presets through `services/session-template-service.ts`                                                                                                                                                                                             |
| `/v1/coaches/me/earnings`                               | `GET`              | `implemented` | invoice-derived earnings snapshot, summary, transactions, and simulated payout/withdrawal state | coach self with active coach profile; sensitive read is audited; caller cannot select another coach                                                                                                   | legacy earnings calculator/report facade; canonical coach money UI continues to use `/v1/invoices*`                                                                                                                                                                       |
| `/v1/coaches/me/payment-instructions`                   | `GET/PATCH`        | `implemented` | saved direct-payment copy for coach invoice/reminder messages                                    | coach self with active coach profile; reads are sensitive/audited; writes audit changed fields without storing audit copies of bank text; does not reuse payout provider bank details                | `components/earnings/coach-payment-instructions-card.tsx`, `services/coach-payment-instructions-service.ts`                                                                                                                                                               |
| `/v1/coaches/me/payout-methods`                         | `GET`              | `implemented` | simulated payout method list                                                                    | coach self with active coach profile; sensitive read is audited; response is API-owned simulated provider state                                                                                       | payout method list through `services/earnings/payout-service.ts`                                                                                                                                                                                                          |
| `/v1/coaches/me/payout-methods`                         | `POST`             | `implemented` | created simulated `PayoutMethod` and refreshed list                                             | coach self with active coach profile; raw sort code/full bank details are not stored or audited; provider remains `simulated` with `providerConfigured=false`                                         | payout method create through `services/earnings/payout-service.ts`                                                                                                                                                                                                        |
| `/v1/coaches/me/payout-methods/:methodId`               | `DELETE`           | `implemented` | refreshed simulated payout method list                                                          | coach self with active coach profile; item must belong to caller; active pending/processing withdrawals block removal; soft-removes the method and audits success/deny paths                         | payout method removal through `services/earnings/payout-service.ts`                                                                                                                                                                                                       |
| `/v1/coaches/me/payout-methods/:methodId/default`       | `PATCH`            | `implemented` | default simulated `PayoutMethod` and refreshed list                                             | coach self with active coach profile; method must belong to caller and be verified                                                                                                                    | payout method default selection through `services/earnings/payout-service.ts`                                                                                                                                                                                             |
| `/v1/coaches/me/withdrawals`                            | `GET`              | `implemented` | simulated withdrawal list, optionally pending only                                              | coach self with active coach profile; sensitive read is audited; no real money provider is invoked                                                                                                    | withdrawal history/pending reads through `services/earnings/payout-service.ts`                                                                                                                                                                                            |
| `/v1/coaches/me/withdrawals`                            | `POST`             | `implemented` | created simulated `Withdrawal` and refreshed list                                               | coach self with active coach profile; payout method must belong to caller and be verified; amount must not exceed backend-derived available balance; provider remains simulated                       | withdrawal requests through `services/earnings/payout-service.ts`                                                                                                                                                                                                         |
| `/v1/coaches/me/withdrawals/:withdrawalId/cancel`       | `POST`             | `implemented` | cancelled simulated `Withdrawal` and refreshed list                                             | coach self with active coach profile; withdrawal must belong to caller and still be pending                                                                                                           | withdrawal cancellation through `services/earnings/payout-service.ts`                                                                                                                                                                                                     |
| `/v1/coaches/me/withdrawals/:withdrawalId/complete`     | `POST`             | `implemented` | completed simulated `Withdrawal` and refreshed list                                             | coach self with active coach profile; withdrawal must belong to caller and be pending/processing; marks simulated completion only                                                                     | simulated payout completion through `services/earnings/payout-service.ts`                                                                                                                                                                                                 |
| `/v1/coaches/offerings`                                 | `GET`              | `implemented` | active `Offering*` index with safe public `coachProfile` metadata                               | authenticated viewer; backend returns active, non-deleted offerings for active coach profiles plus public coach display name, bio, max price, website, social links, experience, languages, specialties, and qualifications | Discover Map coach-card pricing/bookability summary via `services/discover-service.ts` and shared `services/coach-offering-api.ts`                                                                                                                                        |
| `/v1/coaches/:coachId/offerings`                        | `GET`              | `implemented` | active `Offering*` list                                                                         | authenticated viewer; backend returns active, non-deleted offerings for the target coach only                                                                                                         | booking session-type/schedule via `app/book/[coachId]/*`, Discover Map booking fast-track, coach/public profile offering sections, and shared `services/coach-offering-api.ts`                                                                                            |
| `/v1/coaches/:coachId/reviews`                          | `GET`              | `implemented` | verified booking review list                                                                    | public read; backend only returns public reviews tied to completed bookings and authored by the booked guardian/athlete                                                                               | coach public profile review tabs via `services/coach-service.ts`                                                                                                                                                                                                          |
| `/v1/coaches/:coachId/roster`                           | `GET`              | `implemented` | booking-derived coach-athlete roster read model with athlete/guardian labels, session counts, revenue, ratings, and session-note snippets | coach self or privileged admin only; reads derive from backend bookings/participants and are audited as sensitive reads | `app/roster/*`, `hooks/use-athletes-screen.ts`, `services/roster-service.ts`; API mode now reads live roster rows instead of returning an empty list |
| `/v1/coaches/:coachId/roster`                           | `POST`             | `implemented` | durable `CoachAthleteRosterEntry` link for an existing athlete                                    | coach self or privileged admin only; writes require an existing athlete, never create user/athlete/guardian records, and audit success/deny paths as `coach_roster.create` | roster create/assignment flows through `services/roster-service.ts`; API mode now links existing athletes through `/v1` instead of local roster storage |
| `/v1/coaches/:coachId/roster/:athleteId`                | `GET`              | `implemented` | booking-derived coach-athlete roster detail                                                       | coach self or privileged admin only; reads derive from backend booking participation and are audited as sensitive reads | athlete detail through `hooks/use-athlete-detail.ts` and `services/roster-service.ts`; API mode now reads live detail instead of returning null |
| `/v1/coaches/:coachId/roster/:athleteId`                | `PATCH`            | `implemented` | `CoachAthleteRosterEntry` status, tags, focus, and notification overlay update                     | coach self or privileged admin only; writes are scoped to an existing booking-derived roster entry and audit success/deny paths as `coach_roster.update`                                          | athlete detail status, tags, and focus actions through `hooks/use-athlete-detail.ts` and `services/roster-service.ts`; API mode now writes through `/v1` instead of local roster storage |
| `/v1/coaches/:coachId/roster/:athleteId`                | `DELETE`           | `implemented` | `CoachAthleteRosterEntry` soft removal with removal snapshot                                       | coach self or privileged admin only; removal hides the coach-athlete roster relationship, preserves athlete and booking history, and audits success/deny paths as `coach_roster.remove`             | athlete detail remove actions through `hooks/use-athlete-detail.ts` and `services/roster-service.ts`; API mode now writes removal history through `/v1` instead of local roster storage |
| `/v1/coaches/:coachId/roster/:athleteId/notes`          | `POST`             | `implemented` | private `SessionNote` roster note create                                                         | coach self or privileged admin only; writes require a currently visible coach-athlete roster relationship and audit success/deny paths as `coach_roster_note.create`                                  | roster athlete notes through `hooks/use-athlete-detail.ts` and `services/roster-service.ts`; API mode now writes through `/v1` instead of local roster storage                                                                                                             |
| `/v1/coaches/:coachId/roster/:athleteId/notes/:noteId`  | `PATCH/DELETE`     | `implemented` | private `SessionNote` roster note update/soft-remove                                              | coach self or privileged admin only; update/remove require a currently visible coach-athlete roster relationship, only touch coach-owned roster/legacy note rows for the athlete, and audit success/deny paths as `coach_roster_note.update/remove` | roster athlete notes through `hooks/use-athlete-detail.ts` and `services/roster-service.ts`; API mode now updates and soft-removes notes through `/v1` instead of local roster storage                                                                                    |
| `/v1/coaches/:coachId/roster/removals`                  | `GET`              | `implemented` | `CoachAthleteRosterEntry` removal history                                                        | coach self or privileged admin only; returns only roster-link removal history, not hard athlete deletion history, and audits as a sensitive roster read                                             | removal history through `services/roster-service.ts`; API mode now reads removal history from `/v1`                                                                                                                                                                        |
| `/v1/coaches/:coachId/roster/removals/:removalId/undo`  | `POST`             | `implemented` | `CoachAthleteRosterEntry` removal undo                                                           | coach self or privileged admin only; restores the archived roster link if still backed by visible booking participation and audits success/deny paths as `coach_roster.restore`                    | removal undo through `services/roster-service.ts`; API mode now restores removal overlays through `/v1`                                                                                                                                                                    |
| `/v1/coaches/:coachId/verification-status`              | `GET`              | `implemented` | authenticated coach verification status                                                          | authenticated viewer; reads backend `CoachVerification` rows, prefers a still-current approved verification over newer pending evidence, uses `CoachProfile.dbsChecked` only as a legacy DBS fallback, hides document URLs/reviewer notes, and audits sensitive reads | DBS child-booking gate and API-mode coach verification reads through `services/verification-service.ts`                                                                                                                                                                    |
| `/v1/coaches/me/availability/templates`                 | `GET/POST`         | `implemented` | `AvailabilityTemplate*`                                                                         | coach self                                                                                                                                                                                            | `app/(tabs)/availability.tsx`, `app/availability/*`                                                                                                                                                                                                                       |
| `/v1/coaches/me/availability/templates/:templateId`     | `PATCH/DELETE`     | `implemented` | `AvailabilityTemplate*`                                                                         | coach self; item must belong to authenticated coach; delete is a soft remove and is audited                                                                                                           | `app/(tabs)/availability.tsx`, `app/availability/*`                                                                                                                                                                                                                       |
| `/v1/coaches/me/availability/overrides`                 | `GET/POST`         | `implemented` | `AvailabilityOverride*`                                                                         | coach self                                                                                                                                                                                            | `app/availability/calendar.tsx`                                                                                                                                                                                                                                           |
| `/v1/coaches/me/availability/overrides/:overrideId`     | `PATCH/DELETE`     | `implemented` | `AvailabilityOverride*`                                                                         | coach self; item must belong to authenticated coach; delete is a soft remove and is audited                                                                                                           | `app/availability/calendar.tsx`                                                                                                                                                                                                                                           |
| `/v1/coaches/:coachId/availability/templates`           | `GET/POST`         | `implemented` | delegated `AvailabilityTemplate*`                                                               | target coach self or privileged admin only; allow/deny paths are audited; writes preserve target coach ownership and record the acting user separately                                                | delegated/admin availability management through `services/availability-service.ts`; API mode now reads and saves delegated templates through `/v1` instead of local mirrors                                                                                              |
| `/v1/coaches/:coachId/availability/templates/:templateId` | `PATCH/DELETE`     | `implemented` | delegated `AvailabilityTemplate*`                                                               | target coach self or privileged admin only; item must belong to target coach; delete is a soft remove and records the acting user in audit and `deletedByUserId`                                      | delegated/admin availability management through `services/availability-service.ts`; API mode now updates and soft-removes delegated templates through `/v1` when caller context includes the target coach id                                       |
| `/v1/coaches/:coachId/availability/overrides`           | `GET/POST`         | `implemented` | delegated `AvailabilityOverride*`                                                               | target coach self or privileged admin only; allow/deny paths are audited; writes preserve target coach ownership and record the acting user separately                                                | delegated/admin availability management through `services/availability-service.ts`; API mode now reads and saves delegated overrides through `/v1` instead of local mirrors                                                                                              |
| `/v1/coaches/:coachId/availability/overrides/:overrideId` | `PATCH/DELETE`     | `implemented` | delegated `AvailabilityOverride*`                                                               | target coach self or privileged admin only; item must belong to target coach; delete is a soft remove and records the acting user in audit and `deletedByUserId`                                      | delegated/admin availability management through `services/availability-service.ts`; API mode now updates and soft-removes delegated overrides through `/v1`; repeated overrides create individual audited `/v1` overrides when caller context includes the target coach id     |
| `/v1/coaches/:coachId/availability/slots`               | `GET`              | `scaffolded`  | `AvailabilitySlot*`                                                                             | authenticated viewer; current runtime exposes bookable coach slots through backend authority                                                                                                          | `app/book/[coachId]/schedule.tsx`, `hooks/use-multi-week.ts`, `services/availability-service.ts`, `services/invite/session-invite-service.ts`                                                                                                                             |
| `/v1/coaches/me/scheduling-rules`                       | `GET/PATCH`        | `implemented` | `SchedulingRules*`                                                                              | coach self                                                                                                                                                                                            | `services/scheduling-rules-service.ts` consumers                                                                                                                                                                                                                          |
| `/v1/coaches/:coachId/scheduling-rules`                  | `GET`              | `implemented` | current scheduling rules plus active cancellation policy projection                              | authenticated viewer; target coach must exist; returns current/default rules and active policy only, not coach-private policy history; reads are audited                                                | non-self booking validation and refund preview through `services/scheduling-rules-service.ts`; API mode reads `/v1` instead of local mirrors                                                                                                                                |
| `/v1/me/favourite-coaches`                              | `GET`              | `implemented` | favourite coach list                                                                            | authenticated self only; backend ignores client-supplied user identity                                                                                                                                | `app/favourites/index.tsx`, `services/favourite-service.ts`                                                                                                                                                                                                               |
| `/v1/me/favourite-coaches/:coachId`                     | `GET/POST/DELETE`  | `implemented` | favourite coach status/mutation                                                                 | authenticated self only; coach must exist; self-favourite denied; save/remove success and deny paths are audited                                                                                      | `app/favourites/index.tsx`, `services/favourite-service.ts`, coach favourite buttons                                                                                                                                                                                      |
| `/v1/clubs`                                             | `GET`              | `implemented` | `Club*`                                                                                         | authenticated                                                                                                                                                                                         | `app/(tabs)/club-hub.tsx`, `app/club/my-clubs.tsx`, `services/club-authority-service.ts`                                                                                                                                                                                  |
| `/v1/clubs`                                             | `POST`             | `implemented` | `CreateClubResult`                                                                              | authenticated actor becomes owner; duplicate active owner/name is rejected; writes are audited                                                                                                        | club creation flow through `hooks/use-create-club.ts` and `services/club-authority-service.ts`                                                                                                                                                                            |
| `/v1/clubs/:clubId`                                     | `PATCH`            | `implemented` | `Club*`                                                                                         | active club role with `edit_org_profile`; commercial-mode changes require owner; writes and denies are audited                                                                                        | club details, visibility, and commercial settings through `hooks/use-club-settings.ts` and `services/club-authority-service.ts`                                                                                                                                           |
| `/v1/clubs/:clubId`                                     | `DELETE`           | `implemented` | no body                                                                                         | owner or privileged admin only; soft-archives club, memberships, invite codes, and squads; writes and denies are audited as `club.archive`                                                            | club danger-zone archive through `hooks/use-club-settings.ts` and `services/club-authority-service.ts`                                                                                                                                                                    |
| `/v1/clubs/join/resolve`                                | `GET`              | `implemented` | `ResolveClubJoinCodeResponse`                                                                   | authenticated                                                                                                                                                                                         | join-link preview and code validation, `services/club-authority-service.ts`                                                                                                                                                                                               |
| `/v1/clubs/join`                                        | `POST`             | `implemented` | `JoinClubRequest`, `JoinClubResponse`                                                           | authenticated; direct member join or pending staff invite based on invite-code role                                                                                                                   | `app/club/my-clubs.tsx`, `hooks/use-club-hub.ts`, `hooks/use-coach-invites.ts`, `services/club-authority-service.ts`                                                                                                                                                      |
| `/v1/clubs/invites`                                     | `GET`              | `implemented` | `ClubInvitesResponse`                                                                           | authenticated self                                                                                                                                                                                    | `app/coach-invites.tsx`, `hooks/use-coach-invites.ts`, `services/club-authority-service.ts`                                                                                                                                                                               |
| `/v1/clubs/invites/:inviteId/respond`                   | `POST`             | `implemented` | `RespondToClubInviteRequest`, `RespondToClubInviteResponse`                                     | pending invite target only                                                                                                                                                                            | `app/coach-invites.tsx`, `hooks/use-coach-invites.ts`, `services/club-authority-service.ts`                                                                                                                                                                               |
| `/v1/clubs/:clubId/invite-codes`                        | `GET`              | `implemented` | `ClubInviteCodesResponse`                                                                       | club membership                                                                                                                                                                                       | `app/club/settings.tsx`, `hooks/use-club-settings.ts`, `services/club-authority-service.ts`                                                                                                                                                                               |
| `/v1/clubs/:clubId/invite-codes`                        | `POST`             | `implemented` | `CreateClubInviteCodeRequest`, `ClubInviteCode`                                                 | `manage_staff_and_invites` capability                                                                                                                                                                 | `app/club/settings.tsx`, `hooks/use-club-settings.ts`, `services/club-authority-service.ts`                                                                                                                                                                               |
| `/v1/clubs/:clubId/invite-codes/:code`                  | `DELETE`           | `implemented` | none                                                                                            | `manage_staff_and_invites` capability; soft-removes the invite code and audits success/deny paths                                                                                                     | `app/club/settings.tsx`, `hooks/use-club-settings.ts`, `services/club-authority-service.ts`                                                                                                                                                                               |
| `/v1/clubs/:clubId/members`                             | `GET`              | `implemented` | club member list payload                                                                        | privileged admin or active club membership                                                                                                                                                            | club member panels, club settings, event athlete selectors, `services/club-service.ts`                                                                                                                                                                                    |
| `/v1/clubs/:clubId/members/removals`                    | `GET`              | `implemented` | club member removal history payload                                                             | `manage_staff_and_invites`; returns soft-removed membership history and audits success/deny sensitive reads                                                                                           | member removal history and undo surfaces through `services/club-service.ts`                                                                                                                                                                                               |
| `/v1/clubs/:clubId/members/:userId/role`                | `PATCH`            | `implemented` | club member payload                                                                             | `manage_staff_and_invites`; actor must outrank target and assigned role; ownership excluded; writes audited                                                                                           | member management role controls through `services/club-service.ts`                                                                                                                                                                                                        |
| `/v1/clubs/:clubId/members/:userId`                     | `DELETE`           | `implemented` | club member removal payload                                                                     | `manage_staff_and_invites`; actor must outrank target; ownership excluded; soft-removes membership and squad links; writes audited                                                                    | member removal controls through `services/club-service.ts`                                                                                                                                                                                                                |
| `/v1/clubs/:clubId/members/:userId/ban`                 | `POST`             | `implemented` | club member removal payload                                                                     | `manage_staff_and_invites`; actor must outrank target; ownership excluded; writes audited; prior ban blocks rejoin                                                                                    | member ban controls through `services/club-service.ts`                                                                                                                                                                                                                    |
| `/v1/clubs/:clubId/members/removals/:removalId/restore` | `POST`             | `implemented` | club member payload                                                                             | `manage_staff_and_invites`; actor must outrank target; banned removals cannot be restored through undo; writes audited                                                                                | member removal undo through `services/club-service.ts`                                                                                                                                                                                                                    |
| `/v1/clubs/:clubId/branding`                            | `GET/PUT`          | `implemented` | club branding payload                                                                           | read: privileged admin or active club membership; write: `edit_org_profile`; reads and writes audited                                                                                                 | club settings branding editor through `services/club-service.ts`                                                                                                                                                                                                          |
| `/v1/clubs/:clubId/squads/:squadId/members/:userId`     | `PUT/DELETE`       | `implemented` | club member payload with current `squadIds`                                                     | `manage_staff_and_invites`; actor must outrank target; target must resolve to linked athlete; soft-removes squad membership; writes audited                                                           | squad membership toggles through `services/club-service.ts`, `hooks/use-squad-detail.ts`, and `hooks/use-member-management.ts`                                                                                                                                            |
| `/v1/clubs/:clubId/squads`                              | `GET/POST`         | `implemented` | club squad list or squad payload                                                                | read: privileged admin or active club membership; write: `manage_staff_and_invites`; writes audited                                                                                                   | club hub/detail/settings, squad pickers, and create squad through `services/squad-service.ts`                                                                                                                                                                             |
| `/v1/clubs/:clubId/squads/:squadId`                     | `PATCH`            | `implemented` | club squad payload                                                                              | `manage_staff_and_invites`; writes audited                                                                                                                                                            | squad update flows through `services/squad-service.ts`                                                                                                                                                                                                                    |
| `/v1/clubs/:clubId/squads/:squadId`                     | `DELETE`           | `implemented` | 204 archive acknowledgement                                                                     | `manage_staff_and_invites`; blocks active memberships, non-terminal group sessions, and non-terminal matches; writes audited as `club_squad.archive`                                                 | squad archive through `hooks/use-squad-detail.ts` and `services/squad-service.ts`                                                                                                                                                                                         |
| `/v1/squads/:squadId`                                   | `GET`              | `implemented` | club squad payload                                                                              | privileged admin or active membership of the owning club                                                                                                                                              | squad detail, club schedule squad context, squad invite previews through `services/squad-service.ts`                                                                                                                                                                      |
| `/v1/squads/:squadId/members`                           | `GET`              | `implemented` | db-aware squad roster with athlete and guardian/self recipient IDs                              | privileged admin, `manage_staff_and_invites`, or assigned squad owner coach; allowed and denied reads audited                                                                                         | squad invites, match creation, and head-coach oversight through `services/squad-service.ts`                                                                                                                                                                               |
| `/v1/clubs/:clubId/schedule`                            | `GET`              | `implemented` | `ClubScheduleResponse`                                                                          | privileged admin, active club membership, or public club visibility                                                                                                                                   | db-backed `ClubEvent`/`GroupSession`/`ClubMatch` projection for `app/club/[id].tsx`, `app/club/[id]/schedule.tsx`, `app/club/squad/[id]/schedule.tsx`, and `services/club-schedule-service.ts`                                                                            |
| `/v1/clubs/:clubId/schedule/:activityId`                | `GET`              | `implemented` | `ClubActivityDetailResponse`                                                                    | privileged admin, active club membership, or public club visibility                                                                                                                                   | db-backed `ClubEvent`/`GroupSession`/`ClubMatch` projection for `app/club/[id]/activity/[activityId].tsx`, `components/club/ClubScheduleScreen.tsx`, `components/club/ClubActivitiesPanel.tsx`, and `services/club-schedule-service.ts`                                   |
| `/v1/clubs/:clubId/staffing-console`                    | `GET`              | `implemented` | org staffing console payload                                                                    | privileged admin or active club staff with org dashboard/create-session access; reads audit allow/deny paths and project assigned/unassigned club work from backend session/booking truth             | `services/org-staffing-service.ts`, `hooks/use-manage-bookings.ts`, `app/manage/bookings.tsx`                                                                                                                                                                             |
| `/v1/clubs/:clubId/work-assignments/:assignmentId`      | `PATCH`            | `implemented` | updated group-session assignment plus linked booking ids                                        | active club staff assignee; `assign_session_coach` / `reassign_session_coach`; actor must be allowed to assign unassigned or reassign existing club group-session work; writes audit allow/deny paths; propagates only non-cancelled, non-completed linked bookings, syncs their linked invoice coach ownership, and appends booking timeline events | club work assignment/reassignment through `services/org-staffing-service.ts`; current assignment id is the `GroupSession.id`; unassigned work is represented by nullable `GroupSession.coachUserId`                                                                       |
| `/v1/clubs/:clubId/head-coach/oversight`                | `GET`              | `implemented` | head-coach oversight payload                                                                    | privileged admin or active owner/admin/head-coach; owner/admin reads use club scope, head-coach reads are scoped to assigned squads; reads audit allow/deny paths                                     | `services/org-head-coach-service.ts`, `hooks/use-head-coach-oversight.ts`, `app/manage/head-coach.tsx`; projects staff health, completion queues, task-derived athlete watchlist pressure, explicit `HeadCoachTask` rows, and `HeadCoachStandard` rows from backend truth |
| `/v1/clubs/:clubId/head-coach/tasks`                    | `POST`             | `implemented` | head-coach task payload                                                                         | owner/admin/head-coach within oversight scope; target coach and optional booking must be visible in the actor's oversight projection; writes audit actor, target coach, booking/athlete, and due date | issue follow-up and session-note tasks through `services/org-head-coach-service.ts`                                                                                                                                                                                       |
| `/v1/clubs/:clubId/head-coach/tasks/:taskId`            | `PATCH`            | `implemented` | head-coach task payload                                                                         | owner/admin/head-coach within oversight scope; task must already be visible in the actor's oversight projection; writes audit status changes                                                          | mark/reopen head-coach tasks through `services/org-head-coach-service.ts`                                                                                                                                                                                                 |
| `/v1/clubs/:clubId/head-coach/standards`                | `POST`             | `implemented` | head-coach standard payload                                                                     | owner/admin/head-coach with oversight access; writes audit creation                                                                                                                                   | create oversight standards through `services/org-head-coach-service.ts`                                                                                                                                                                                                   |
| `/v1/clubs/:clubId/head-coach/standards/:standardId`    | `PATCH`            | `implemented` | head-coach standard payload                                                                     | owner/admin/head-coach with oversight access; writes audit active state changes                                                                                                                       | toggle oversight standards through `services/org-head-coach-service.ts`                                                                                                                                                                                                   |
| `/v1/clubs/:clubId/owner-dashboard`                     | `GET`              | `implemented` | owner dashboard summary, finance, staffing, oversight, and support payload                      | owner/admin or privileged admin; composes staffing-console, head-coach oversight, backend invoice totals, and unresolved booking-linked trust incidents with audited sensitive reads                  | `services/org-owner-dashboard-service.ts`, `hooks/use-club-dashboard.ts`, `app/club/[clubId]/dashboard.tsx`                                                                                                                                                               |
| `/v1/clubs/:clubId/matches`                             | `GET`              | `implemented` | club match list payload                                                                         | privileged admin, active club membership, or public club visibility                                                                                                                                   | recent results, match lists, and schedule projections through `services/match-service.ts`                                                                                                                                                                                 |
| `/v1/clubs/:clubId/matches`                             | `POST`             | `implemented` | club match payload                                                                              | active club staff or privileged admin; actor/coach derived from auth; writes are audited                                                                                                              | match creation through `hooks/use-create-match.ts` and `services/match-service.ts`                                                                                                                                                                                        |
| `/v1/matches/:matchId`                                  | `GET`              | `implemented` | club match payload                                                                              | privileged admin, active club membership, or public club visibility                                                                                                                                   | match detail through `services/match-service.ts`                                                                                                                                                                                                                          |
| `/v1/matches/:matchId/players/invite`                   | `POST`             | `implemented` | club match payload with selected players                                                        | active club staff or privileged admin; athletes must exist, have a linked guardian, and belong to the match squad when scoped; writes and denies are audited                                          | match player invite fan-out through `services/match-service.ts`                                                                                                                                                                                                           |
| `/v1/matches/:matchId/players/respond`                  | `POST`             | `implemented` | club match payload with availability response                                                   | invited guardian, linked athlete, or privileged admin; active club visibility still required; writes and denies are audited                                                                           | parent/player availability responses through `services/match-service.ts`                                                                                                                                                                                                  |
| `/v1/matches/:matchId/lineup`                           | `PATCH`            | `implemented` | club match payload with selected/reserve players                                                | active club staff or privileged admin; lineup athletes must already be invited; writes and denies are audited                                                                                         | coach lineup selection through `services/match-service.ts`                                                                                                                                                                                                                |
| `/v1/matches/:matchId/result`                           | `PATCH`            | `implemented` | club match payload                                                                              | active club staff or privileged admin; result writes are audited                                                                                                                                      | result recording and homepage recent-result reads through `services/match-service.ts`                                                                                                                                                                                     |
| `/v1/matches/:matchId/status`                           | `PATCH`            | `implemented` | club match payload                                                                              | active club staff or privileged admin; status writes are audited                                                                                                                                      | match cancellation/status updates through `services/match-service.ts`                                                                                                                                                                                                     |
| `/v1/clubs/:clubId/integrations`                        | `GET/POST/PATCH`   | `planned`     | `ClubIntegration*`                                                                              | `club_admin`                                                                                                                                                                                          | future club settings/integrations                                                                                                                                                                                                                                         |
| `/v1/clubs/:clubId/matches/import`                      | `POST`             | `planned`     | `ImportClubMatchesRequest`, `ImportClubMatchesResponse`                                         | `club_admin`                                                                                                                                                                                          | future club schedule admin tools                                                                                                                                                                                                                                          |
| `/v1/coaches/me/verifications/:type/documents`          | `GET`              | `implemented` | private coach verification document evidence list                                               | coach self; reads seed/db backend verification documents with media object metadata and audits sensitive reads                                                                                        | coach verification evidence surfaces via `services/verification-service.ts`                                                                                                                                                                                               |
| `/v1/coaches/me/verifications/:type/documents`          | `POST`             | `implemented` | `mediaObjectId` plus optional `fileLabel`; attaches an existing private upload as pending verification evidence | coach self only; media object must belong to the authenticated coach, be active, scanned/available, and be an image or document; writes/reuses a pending `CoachVerification`, links `VerificationDocument`, and audits success/deny paths | `app/verification/*`, coach verification evidence surfaces via `services/verification-service.ts`                                                                                                                                                                          |

## Booking / Group Sessions / Invites / Events

| Route                                                        | Method     | Status        | Contract(s)                                                                   | AuthZ                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | UI anchors                                                                                                                                                    |
| ------------------------------------------------------------ | ---------- | ------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/v1/bookings`                                               | `GET`      | `scaffolded`  | `BookingListResponse` with nullable booking `recurringSeriesId`/`groupSessionId` | participants/guardian/coach                                                                                                                                                                                                                                                                                                                                                                                                                                              | `app/(tabs)/bookings/index.tsx`, `hooks/use-bookings.ts`, `services/booking/booking-authority-service.ts`                                                     |
| `/v1/bookings`                                               | `POST`     | `scaffolded`  | `CreateBookingRequest`, `BookingResponse`                                     | actor must pass repository athlete-relationship authz; backend re-validates the selected slot before create and honors same-key idempotency replay/conflict                                                                                                                                                                                                                                                                                                              | `app/book/[coachId]/*`, `components/ui/booking/*`, `services/booking/booking-authority-service.ts`; client CRUD keeps runtime memory only and does not create API-mode local notification overlays                         |
| `/v1/bookings/:bookingId`                                    | `GET`      | `scaffolded`  | `BookingResponse` with nullable `recurringSeriesId`/`groupSessionId`           | participants/guardian/coach                                                                                                                                                                                                                                                                                                                                                                                                                                              | `app/(tabs)/bookings/[id].tsx`, `hooks/use-booking-detail.ts`, `services/booking/booking-authority-service.ts`                                                |
| `/v1/bookings/:bookingId`                                    | `PATCH`    | `implemented` | `UpdateBookingRequest`, `BookingResponse`                                     | assigned coach, booking owner, or participant guardian for every athlete; validates future schedule changes against coach availability while excluding the edited booking, checks expected version, writes no-status-change timeline events, honors idempotency replay, and audits success/deny paths; does not reassign coach, mutate participants, or move money | booking detail edits through `services/booking/booking-authority-service.ts`; API mode detail updates use this route, while unsupported local-only fields still fail closed |
| `/v1/bookings/:bookingId/cancel`                             | `POST`     | `scaffolded`  | `CancelBookingRequest`, `BookingResponse`                                     | parent/athlete/coach tied to booking participants or delivery coach                                                                                                                                                                                                                                                                                                                                                                                                      | `app/booking/[id]/cancel.tsx`, `hooks/use-booking-cancel.ts`, `services/booking/booking-authority-service.ts`                                                 |
| `/v1/cancellation-records`                                   | `GET`      | `implemented` | cancellation record list/stat source projected from cancelled `Booking` rows   | scoped coach, booked guardian/athlete for own booking, or privileged admin; sensitive reads are audited and do not imply refund authority; refund display is derived from booking price and cancellation fee minor units                                                                                                                                                                                                                                                   | `services/cancellation-service.ts`; live API mode reads this route and derives stats from returned records                                                     |
| `/v1/cancellation-records/:bookingId`                        | `GET`      | `implemented` | cancellation record lookup by booking projected from cancelled `Booking` rows  | scoped booking actor or privileged admin; sensitive reads and denied reads are audited; visible non-cancelled bookings return `record: null`                                                                                                                                                                                                                                                                                                                              | `services/cancellation-service.ts`; live API mode reads this route after booking cancellation                                                                   |
| `/v1/families/:familyId/no-shows`                            | `GET`      | `implemented` | family no-show count derived from backend attendance proof                    | linked family guardian or privileged admin; reads are audited and de-duplicate matching attendance-record and group-registration no-show proof                                                                                                                                                                                                                                                                                                                           | `services/cancellation-service.ts#getNoShowCount`; live API mode reads backend proof instead of local counters                                                |
| `/v1/families/:familyId/no-shows`                            | `PATCH`    | `implemented` | proof-backed family no-show record/correction with refreshed count output    | linked guardian, assigned coach with booking or group-registration proof, or privileged admin; mutations require explicit athlete proof, never accept bare counter writes, preserve corrected proof rows, and are audited                                                                                                                                                                                                                                                                                         | `services/cancellation-service.ts#incrementNoShow`, `resetNoShowCount`; API mode records/clears no-shows only when a booking or group-registration proof object is supplied; bare counter writes still fail closed         |
| `/v1/bookings/:bookingId/reopen`                             | `POST`     | `scaffolded`  | `ReopenBookingRequest`, `BookingResponse`                                     | parent/athlete/coach tied to booking participants or delivery coach                                                                                                                                                                                                                                                                                                                                                                                                      | `app/(tabs)/bookings/[id].tsx`, `hooks/use-booking-detail.ts`, `services/booking/booking-authority-service.ts`                                                |
| `/v1/bookings/:bookingId/confirm`                            | `POST`     | `implemented` | `ConfirmBookingRequest`, `BookingResponse`                                    | assigned delivery coach only; only `PENDING`/`AWAITING_CONFIRMATION` bookings transition, already-confirmed bookings replay as no-op                                                                                                                                                                                                                                                                                                                                     | `services/booking/booking-authority-service.ts`, `services/booking/booking-status-service.ts`                                                                  |
| `/v1/bookings/:bookingId/complete`                           | `POST`     | `implemented` | `CompleteBookingRequest`, `BookingResponse`; optional `attendance[]` rows carry `athleteId`, `ATTENDED`/`NO_SHOW`, notes, and effort rating | assigned delivery coach only; completion rejects non-participant attendance rows, writes backend attendance proof for each participant, creates public progress-visible session-note proof only for attended athletes when a coach note is present, and audits success/deny/error outcomes with attendance counts                                                                                                                                                       | `app/session/[id]/complete.tsx`, `hooks/use-session-completion.ts`, coach completion queue, attendance/progress proof linkage                                 |
| `/v1/bookings/:bookingId/session-note`                       | `GET`      | `implemented` | booking-scoped session note payload                                           | visible booking actor; coach-only notes are hidden from non-coach actors; success and deny paths are audited                                                                                                                                                                                                                                                                                                                                                              | `app/session-notes/[bookingId].tsx`, `hooks/use-session-note.ts`, `services/progress/progress-feedback-service.ts`                                            |
| `/v1/bookings/:bookingId/session-note`                       | `PUT`      | `implemented` | booking-scoped session note payload                                           | assigned delivery coach only; writes `SessionNote` rows for booking participants and audits success/deny paths                                                                                                                                                                                                                                                                                                                                                            | `app/session-notes/[bookingId].tsx`, `hooks/use-session-note.ts`, `services/progress/progress-feedback-service.ts`                                            |
| `/v1/bookings/:bookingId/reviews/me`                         | `GET`      | `implemented` | booking review status response                                                | booked guardian/athlete only; unrelated actors are denied                                                                                                                                                                                                                                                                                                                                                                                                                | `app/(tabs)/bookings/[id].tsx`, `app/review/[bookingId].tsx`, `services/review-sync-service.ts`                                                               |
| `/v1/bookings/:bookingId/reviews`                            | `POST`     | `implemented` | booking review payload/response                                               | booked guardian/athlete only; booking must be `COMPLETED`; duplicate submits replay the existing verified review; success and deny paths are audited                                                                                                                                                                                                                                                                                                                     | `app/review/[bookingId].tsx`, coach public review/proof surfaces                                                                                              |
| `/v1/booking-series`                                         | `GET`      | `implemented` | `BookingSeriesListResponse`                                                   | visible to delivery coach, booked-by guardian, linked guardian, or athlete participant                                                                                                                                                                                                                                                                                                                                                                                   | `services/multi-week-booking-service.ts`, `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                            |
| `/v1/booking-series`                                         | `POST`     | `implemented` | `CreateBookingSeriesRequest`, `CreateBookingSeriesResponse`                   | actor must pass athlete-relationship authz; backend validates every occurrence slot and replays same-key idempotent creates                                                                                                                                                                                                                                                                                                                                              | `hooks/use-multi-week.ts`, `services/multi-week-booking-service.ts`, `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts` |
| `/v1/booking-series/:seriesId`                               | `GET`      | `implemented` | `BookingSeriesResponse`                                                       | same visibility gate as series list                                                                                                                                                                                                                                                                                                                                                                                                                                      | `services/multi-week-booking-service.ts`, `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                            |
| `/v1/booking-series/:seriesId`                               | `PATCH`    | `implemented` | `UpdateBookingSeriesRequest`, `UpdateBookingSeriesResponse`                   | read visibility is not mutation authority; updates require booked-by user or a guardian linked to every athlete; expected-version conflict checks; updates future linked bookings only; mutable linked invoices are backend-synced while paid/void/written-off invoices block                                                                                                                              | `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                                                                      |
| `/v1/booking-series/:seriesId/cancel`                        | `POST`     | `implemented` | `CancelBookingSeriesRequest`, `CancelBookingSeriesResponse`                   | read visibility is not mutation authority; cancellation requires booked-by user or a guardian linked to every athlete; expected-version conflict checks; linked open invoices are voided and active payment attempts are canceled                                                                                                                                                                           | `services/multi-week-booking-service.ts`, `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                            |
| `/v1/booking-series/:seriesId/pause`                         | `POST`     | `implemented` | `PauseBookingSeriesRequest`, `PauseBookingSeriesResponse`                     | read visibility is not mutation authority; pause requires booked-by user or a guardian linked to every athlete; expected-version conflict checks; existing linked bookings stay unchanged                                                                                                                                                                                                                  | `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                                                                      |
| `/v1/booking-series/:seriesId/resume`                        | `POST`     | `implemented` | `ResumeBookingSeriesRequest`, `ResumeBookingSeriesResponse`                   | read visibility is not mutation authority; resume requires booked-by user or a guardian linked to every athlete; expected-version conflict checks; series status is re-derived from linked booking state                                                                                                                                                                                                   | `services/recurring-booking-service.ts`, `services/booking/booking-authority-service.ts`                                                                      |
| `/v1/group-sessions`                                         | `GET`      | `implemented` | `GroupSession*`                                                               | read visibility for coach, participant guardian/athlete, invited target, or `club_admin`                                                                                                                                                                                                                                                                                                                                                                                 | `app/group-sessions/index.tsx`, `hooks/use-group-sessions.ts`, `services/group-session/group-session-authority-service.ts`                                    |
| `/v1/group-sessions`                                         | `POST`     | `implemented` | `GroupSession*`                                                               | coach self-create; when `clubId` is supplied, actor must have active club authority to create organisation sessions; assigning another coach requires `assign_session_coach` and an active club-staff target; `squadId` requires the supplied club and must belong to it                                                                                                                                                                                                 | `app/group-sessions/create.tsx`, `services/group-session/group-session-authority-service.ts`                                                                  |
| `/v1/group-sessions/:sessionId`                              | `GET`      | `implemented` | `GroupSession*`                                                               | same read visibility as the list route                                                                                                                                                                                                                                                                                                                                                                                                                                   | group session detail + `services/group-session/group-session-authority-service.ts`                                                                            |
| `/v1/group-sessions/:sessionId/publish`                      | `PATCH`    | `implemented` | `GroupSession*`                                                               | coach owner or privileged admin                                                                                                                                                                                                                                                                                                                                                                                                                                          | coach session management surfaces + `services/group-session/group-session-authority-service.ts`                                                               |
| `/v1/group-sessions/:sessionId/off-platform-attendees`       | `PATCH`    | `implemented` | `UpdateGroupSessionOffPlatformRequest`, `GroupSession*`                       | session coach owner or privileged admin; writes `GroupSession.offPlatformParticipants`, derives full/published capacity from registered plus off-platform headcount, and audits allow/deny paths                                                                                                                                                                                                                                                                          | session detail off-platform attendee controls through `hooks/use-session-detail-modal.ts` and `services/group-session/group-session-authority-service.ts`     |
| `/v1/group-sessions/:sessionId/instances/cancel`             | `PATCH`    | `implemented` | `CancelGroupSessionInstanceRequest`, `GroupSession*`                          | session coach owner or privileged admin; records cancelled instance dates in `GroupSession.cancelledInstancesJson` without deleting the base schedule, and audits allow/deny paths                                                                                                                                                                                                                                                                                       | recurring instance manager through `hooks/use-session-detail-modal.ts` and `services/group-session/group-session-authority-service.ts`                         |
| `/v1/group-sessions/:sessionId/series/end`                   | `PATCH`    | `implemented` | `EndGroupSessionSeriesRequest`, `GroupSession*`                               | session coach owner or privileged admin; records all scheduled dates from `fromDate` in `GroupSession.cancelledInstancesJson` without deleting the base schedule, and audits allow/deny paths                                                                                                                                                                                                                                                                            | end recurring series action through `hooks/use-session-detail-modal.ts` and `services/group-session/group-session-authority-service.ts`                         |
| `/v1/group-sessions/:sessionId/cancel`                       | `PATCH`    | `implemented` | `GroupSession*`                                                               | coach owner or privileged admin; cancels active registrations and linked bookings, voids open linked invoices/payment attempts, blocks paid invoices until refund approval, detaches attendance proof, and resets counters                                                                                                                                                                                                                                               | coach session management surfaces + `services/group-session/group-session-authority-service.ts`                                                               |
| `/v1/group-sessions/:sessionId/register`                     | `POST`     | `implemented` | `RegisterGroupSessionRequest`, `RegisterGroupSessionResponse`                 | athlete self, guardian of athlete, or `club_admin` acting on behalf; session must be `PUBLISHED` or `FULL`; billable registrations create linked booking and invoice, with `paidAt` set only after backend payment confirmation; writes registration audit events                                                                                                                                                                                                        | `hooks/use-group-session.ts`, `services/group-session/session-registration-service.ts`                                                                        |
| `/v1/group-sessions/:sessionId/roster`                       | `GET`      | `implemented` | `GroupSession*`, `GroupRegistration*`                                         | visible coach owner, participant guardian/athlete, invited target, or privileged admin                                                                                                                                                                                                                                                                                                                                                                                   | coach roster and attendance surfaces, `services/group-session/session-registration-service.ts`                                                                |
| `/v1/group-sessions/:sessionId/waitlist`                     | `POST`     | `implemented` | `JoinGroupSessionWaitlistRequest`, `RegisterGroupSessionResponse`             | athlete self, guardian of athlete, or `club_admin` acting on behalf; session must be full and waitlist-enabled; creates or returns a waitlisted registration without a booking or invoice; denies waitlist attempts while spaces remain; writes waitlist audit events                                                                                                                                                                                                     | group session detail waitlist action via `hooks/use-group-session.ts`, `services/group-session/session-registration-service.ts`                                |
| `/v1/group-session-registrations`                            | `GET`      | `implemented` | `GroupRegistration*`                                                          | guardian/athlete scoped to each requested athlete or privileged admin                                                                                                                                                                                                                                                                                                                                                                                                    | family training history + `services/group-session/session-registration-service.ts`                                                                            |
| `/v1/group-session-registrations/:registrationId`            | `DELETE`   | `implemented` | none                                                                          | soft cancellation, not hard athlete deletion; session coach owner, registered guardian/athlete, or privileged admin; linked open invoices are voided and paid invoices require backend refund approval before cancellation; waitlist promotion creates the promoted athlete's billable booking/invoice chain; writes cancellation audit events                                                                                                                            | registration cancellation + invite acceptance cleanup via `services/group-session/session-registration-service.ts`                                            |
| `/v1/group-session-registrations/:registrationId/attendance` | `PATCH`    | `implemented` | `GroupRegistration*`                                                          | session coach owner or privileged admin; attendance mark/clear writes backend audit events                                                                                                                                                                                                                                                                                                                                                                               | coach roster attendance surfaces + `services/group-session/session-registration-service.ts`                                                                   |
| `/v1/group-sessions/:sessionId/rsvps`                        | `GET/POST` | `implemented` | `SessionRsvp*`                                                                | visible session actors can read scoped RSVPs; session coach/club staff can read all; parent/self actors can create their own pending RSVP for linked athletes; writes are audited                                                                                                                                                                                                                                                                                        | `app/session/[id]/rsvp.tsx`, `hooks/use-group-session.ts`, `services/rsvp-service.ts`                                                                         |
| `/v1/group-sessions/:sessionId/rsvps/counts`                 | `GET`      | `implemented` | `SessionRsvpCounts`                                                           | same session RSVP read gate; derives counts from `SessionRsvp` in seed/db-fixture/db modes                                                                                                                                                                                                                                                                                                                                                                               | RSVP summary and group-session detail bars through `services/rsvp-service.ts`                                                                                 |
| `/v1/group-sessions/:sessionId/rsvps/remind`                 | `POST`     | `implemented` | reminder count                                                                | session coach, active club staff, or privileged admin; queues `Notification` rows for pending RSVPs and audits allow/deny paths                                                                                                                                                                                                                                                                                                                                          | coach RSVP reminder action through `services/rsvp-service.ts`                                                                                                 |
| `/v1/group-sessions/:sessionId/rsvps`                        | `DELETE`   | `implemented` | removed count plus legacy `deleted` alias                                      | session coach, active club staff, or privileged admin; soft-removes session RSVP rows and audits allow/deny paths as `session_rsvp.remove`                                                                                                                                                                                                                                                                                                                               | session cancellation cleanup through `services/rsvp-service.ts`                                                                                               |
| `/v1/session-rsvps`                                          | `GET`      | `implemented` | `SessionRsvp*`                                                                | authenticated user self-list or privileged admin; optional status filter                                                                                                                                                                                                                                                                                                                                                                                                 | parent pending RSVP surfaces through `services/rsvp-service.ts`                                                                                               |
| `/v1/session-rsvps/counts`                                   | `GET`      | `implemented` | `SessionRsvpCountsBySession`                                                  | same per-session RSVP read gates for each requested session                                                                                                                                                                                                                                                                                                                                                                                                              | batch RSVP count reads through `services/rsvp-service.ts`                                                                                                     |
| `/v1/session-rsvps/:rsvpId`                                  | `GET`      | `implemented` | `SessionRsvp*`                                                                | RSVP owner, session coach/club staff, or privileged admin                                                                                                                                                                                                                                                                                                                                                                                                                | RSVP deep links through `app/session/[id]/rsvp.tsx`                                                                                                           |
| `/v1/session-rsvps/:rsvpId/respond`                          | `PATCH`    | `implemented` | `SessionRsvp*`                                                                | RSVP owner or privileged admin only; session staff cannot impersonate parent attendance intent; writes are audited                                                                                                                                                                                                                                                                                                                                                       | parent RSVP buttons through `services/rsvp-service.ts`                                                                                                        |
| `/v1/invites`                                                | `POST`     | `scaffolded`  | scaffold session-invite create payload + invite snapshot                      | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; owner coach self or `club_admin` acting on behalf; backend honors same-key idempotency replay/conflict, validates direct-invite proposed slots and pending-hold conflicts before create, validates `SQUAD_ONLY` `squadIds` against active squad membership, persists create state, and audits allowed and denied creates                                                                                 | `app/sessions/create.tsx`, `hooks/use-create-session.ts`, `services/invite/session-invite-authority-service.ts`, `services/invite/bulk-invite-service.ts`                                               |
| `/v1/invites`                                                | `GET`      | `scaffolded`  | scaffold session-invite list response                                         | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; owner coach sent view or invite target received view for authenticated self; `SQUAD_ONLY` fallback discovery by `squadIds` requires linked guardian/athlete squad membership instead of trusting guessed squad ids                                                                                                                                                                                    | `app/session-invites/index.tsx`, `services/invite/session-invite-authority-service.ts`, `services/invite/squad-invite-service.ts`                                                                        |
| `/v1/invites/:inviteId`                                      | `GET`      | `scaffolded`  | scaffold session-invite detail response                                       | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; owner coach or invite target only                                                                                                                                                                                                                                                                                                                                                                    | `app/session-invites/[id].tsx`, `services/invite/session-invite-authority-service.ts`                                                                         |
| `/v1/invites/:inviteId`                                      | `DELETE`   | `scaffolded`  | none                                                                          | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; owner coach or `club_admin`; allowed and denied cancellations are audited                                                                                                                                                                                                                                                                                                                            | `app/session-invites/index.tsx`, `app/session-invites/[id].tsx`, `services/invite/session-invite-authority-service.ts`                                        |
| `/v1/invites/:inviteId/remind`                               | `POST`     | `scaffolded`  | none                                                                          | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; owner coach or `club_admin`; allowed and denied reminders are audited                                                                                                                                                                                                                                                                                                                                | `app/session-invites/[id].tsx`, `services/invite/session-invite-authority-service.ts`                                                                         |
| `/v1/invites/:inviteId/dismiss`                              | `POST`     | `scaffolded`  | none                                                                          | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; invite target only; allowed and denied dismissals are audited                                                                                                                                                                                                                                                                                                                                        | `app/session-invites/index.tsx`, `services/invite/session-invite-authority-service.ts`                                                                        |
| `/v1/invites/:inviteId/respond`                              | `POST`     | `scaffolded`  | `InviteResponseRequest`, `InviteResponseResult` plus scaffold invite snapshot | seed/db-fixture plus real db `Invite`/`InviteTarget` route adapter; invite target only; backend re-validates accepted direct-invite slots, creates direct-invite bookings through booking repository authority in db mode, creates recurring booking series and accepted-week bookings for `recurringWeekResponses`, persists terminal response state, replays same terminal responses, rejects accept/decline flips after response, and audits allowed/denied responses | `app/session-invites/*`, `services/invite/session-invite-authority-service.ts`                                                                                |
| `/v1/invites/:inviteId/rsvps`                                | `GET`      | `implemented` | invite RSVP responses and counts                                              | invite owner, privileged admin, invite target, or authenticated open-invite viewer; reads current invite social RSVP metadata and audits allow/deny paths                                                                                                                                                                                                                                                                                                                | `app/session-invites/[id].tsx`, `services/invite/invite-rsvp-service.ts`                                                                                      |
| `/v1/invites/:inviteId/rsvps`                                | `POST`     | `implemented` | invite RSVP response, full response list, and counts                          | invite target or authenticated open-invite viewer; invite owners cannot impersonate attendee intent; optional child-scoped writes require target/guardian relationship; writes current invite social RSVP metadata and audits allow/deny paths                                                                                                                                                                                                                           | `app/session-invites/[id].tsx`, `services/invite/invite-rsvp-service.ts`                                                                                      |
| `/v1/invite-rsvps/:responseId`                               | `PATCH`    | `implemented` | invite RSVP response, full response list, and counts                          | response owner only, still gated by invite target/open access; updates social RSVP status and audits allow/deny paths                                                                                                                                                                                                                                                                                                                                                    | `services/invite/invite-rsvp-service.ts`                                                                                                                      |
| `/v1/events/:eventId/rsvp`                                   | `POST`     | `implemented` | `EventRsvpRequest`                                                            | active club member or privileged admin; real `db` mode upserts `EventRsvp` through Prisma and audits allow/deny paths, seed/db-fixture modes retain mutable test-store behavior                                                                                                                                                                                                                                                                                          | `hooks/use-event-rsvp.ts`, `services/event/event-rsvp-service.ts`                                                                                             |
| `/v1/events/:eventId/rsvps`                                  | `GET`      | `implemented` | event RSVP list                                                               | active club member or privileged admin; real `db` mode reads `EventRsvp` through Prisma and audits allow/deny reads                                                                                                                                                                                                                                                                                                                                                      | event detail/attendee surfaces through `services/event/event-rsvp-service.ts`                                                                                 |
| `/v1/events/:eventId/rsvps/:userId`                          | `GET`      | `implemented` | user event RSVP                                                               | active club member or privileged admin; real `db` mode reads `EventRsvp` through Prisma and audits allow/deny reads                                                                                                                                                                                                                                                                                                                                                      | event detail/RSVP screens through `services/event/event-rsvp-service.ts`                                                                                      |
| `/v1/events/:eventId/rsvps/remind`                           | `POST`     | `implemented` | queued reminder count                                                         | event staff or privileged admin; seed/db-fixture modes append backend-visible `Notification` rows, real `db` mode writes Prisma `Notification` rows, and allowed/denied fan-out is audited                                                                                                                                                                                                                                                                               | `services/event/event-rsvp-service.ts`                                                                                                                        |
| `/v1/events/:eventId/attendance`                             | `GET`      | `implemented` | event attendance list                                                         | event staff or privileged admin; reads persisted `EventAttendance` rows in db mode and audits because they expose event presence                                                                                                                                                                                                                                                                                                                                         | `hooks/use-event-attendees.ts`, `services/event/event-attendance-service.ts`                                                                                  |
| `/v1/events/:eventId/attendance/:userId`                     | `GET`      | `implemented` | user event attendance record                                                  | self active club member, event staff, or privileged admin; reads persisted `EventAttendance` rows in db mode and audits allow/deny paths                                                                                                                                                                                                                                                                                                                                 | event detail/check-in surfaces, `services/event/event-attendance-service.ts`                                                                                  |
| `/v1/events/:eventId/checkins`                               | `POST`     | `implemented` | event check-in create/update                                                  | self check-in for active club members during the valid event window, event staff, or privileged admin; writes persisted `EventAttendance` rows and audits actor, target user, method, guests, and location-validation metadata                                                                                                                                                                                                                                           | event detail/check-in surfaces, `services/event/event-attendance-service.ts`                                                                                  |
| `/v1/events/:eventId/checkins/:userId`                       | `DELETE`   | `implemented` | event check-in removal                                                        | event staff or privileged admin; removals soft-remove persisted `EventAttendance` rows with actor metadata, active attendance reads hide removed rows, re-check-in reactivates the row, and writes audit actor, target user, and whether a row existed                                                                                                                                                                                                                | event detail/check-in surfaces, `services/event/event-attendance-service.ts`; product copy should say remove check-in, not delete attendee                    |
| `/v1/events/:eventId/attendance/stats`                       | `GET`      | `implemented` | event attendance statistics                                                   | event staff or privileged admin; derives RSVP counts from `EventRsvp`, check-in totals from `EventAttendance`, and audits allow/deny reads because stats expose event presence                                                                                                                                                                                                                                                                                           | event detail/attendee surfaces, `services/event/event-attendance-service.ts`                                                                                  |
| `/v1/clubs/:clubId/events`                                   | `GET`      | `implemented` | club event list                                                               | active club members can list non-draft club events; athlete-targeted events require linked athlete/guardian assignment; active club staff and privileged admins can include drafts; reads `ClubEvent` through Prisma in db mode and audits allow/deny paths                                                                                                                                                                                                                 | event list/upcoming-event reads through `services/event/event-crud-service.ts`                                                                                |
| `/v1/clubs/:clubId/events`                                   | `POST`     | `implemented` | club event create                                                             | active club staff or privileged admin; creates draft `ClubEvent` rows through Prisma in db mode; optional `athleteIds` must be active athletes assigned to the event club and are stored in event metadata/visibility before publish; audits allow/deny writes                                                                                                                                                                                                                 | event creation flow through `services/event/event-crud-service.ts`                                                                                            |
| `/v1/events/:eventId`                                        | `GET`      | `implemented` | club event detail                                                             | active club members can read non-draft club events; athlete-targeted events require linked athlete/guardian assignment; draft reads require event staff or privileged admin; reads `ClubEvent` through Prisma in db mode and audits allow/deny paths                                                                                                                                                                                                                        | event detail through `services/event/event-crud-service.ts`                                                                                                   |
| `/v1/events/:eventId`                                        | `PATCH`    | `implemented` | club event update/status                                                      | active club staff or privileged admin; updates `ClubEvent` fields/status through Prisma in db mode; optional `athleteIds` must be active athletes assigned to the event club and recalculate athlete-scoped visibility; audits allow/deny writes                                                                                                                                                                                                                             | event update, publish/cancel flows through `services/event/event-crud-service.ts`                                                                             |
| `/v1/events/:eventId/invites/club`                           | `POST`     | `implemented` | invite active club members to event                                           | active club staff or privileged admin; queues backend-visible `Notification` rows for active club members and audits invite fan-out                                                                                                                                                                                                                                                                                                                                      | event publish/invite flow through `services/event/event-crud-service.ts`                                                                                      |
| `/v1/events/:eventId/invites/squads`                         | `POST`     | `implemented` | invite primary guardians for active athletes in selected event-club squads    | active club staff or privileged admin; requested squads must belong to the event club; queues backend-visible `Notification` rows for unique primary guardians and audits allow/deny fan-out                                                                                                                                                                                                                                                                             | squad event invite flow through `services/invite/event-invite-service.ts` and `services/event/event-crud-service.ts`                                          |
| `/v1/events/:eventId/invites/athletes`                       | `POST`     | `implemented` | invite linked athlete accounts and guardians for selected event-club athletes | active club staff or privileged admin; requested athletes must be active and assigned to the event club; stamps athlete-scoped event metadata/visibility, queues backend-visible `Notification` rows for linked athlete accounts and guardians, and audits allow/deny fan-out                                                                                                                                                                                               | specific-athlete event publish/invite flow through `services/event/event-crud-service.ts`                                                                      |

## Revenue / Reconciler

| Route                                                | Method | Status        | Contract(s)                          | AuthZ                                                                       | UI anchors                                                                                 |
| ---------------------------------------------------- | ------ | ------------- | ------------------------------------ | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `/v1/invoices`                                       | `GET`  | `scaffolded`  | `InvoiceListResponse`                | authenticated self; filtered to owner coach / payer / privileged admin / active club owner-admin finance role for linked club invoices | `app/invoices/index.tsx`, `app/(tabs)/bookings/[id].tsx`, `services/invoice-service.ts`    |
| `/v1/invoices/:invoiceId`                            | `GET`  | `scaffolded`  | `InvoiceResponse`                    | owner coach / payer / privileged admin / active club owner-admin finance role for linked club invoices | `components/invoices/*`, `hooks/use-invoice-detail.ts`, `services/invoice-service.ts`      |
| `/v1/coaches/me/invoices`                            | `GET`  | `implemented` | `InvoiceListResponse`                | coach self; backend derives `coachId` from authenticated actor, supports status/booking/date filters, and audits sensitive finance reads | `app/(tabs)/earnings.tsx`, analytics revenue                                               |
| `/v1/invoices/generate`                              | `POST` | `implemented` | `GenerateInvoiceRequest`             | coach/self service flow                                                     | `services/invoice-service.ts`, coach invoice generation                                    |
| `/v1/invoices/:invoiceId/payments`                   | `POST` | `implemented` | `CreateInvoicePaymentSessionRequest` | payer self or privileged admin                                              | hosted payer checkout/session creation from invoice detail                                 |
| `/v1/invoices/:invoiceId/refunds`                    | `POST` | `implemented` | `InvoiceRefundRequest`               | owner coach / privileged admin / active club owner-admin finance role for linked club invoices | paid-booking cancellation unlock after verified full refund                                |
| `/v1/invoices/:invoiceId/mark-paid`                  | `POST` | `implemented` | `InvoiceTransitionRequest`, optional manual receipt fields | owner coach / privileged admin / active club owner-admin finance role for linked club invoices; records structured manual receipt metadata, cancels active hosted payment attempts, and requires manual amount to match invoice total | reconciler UI, `hooks/use-session-payments.ts`, `components/invoices/mark-paid-button.tsx` |
| `/v1/invoices/:invoiceId/mark-unpaid`                | `POST` | `implemented` | `InvoiceTransitionRequest`           | owner coach / privileged admin / active club owner-admin finance role for linked club invoices; manual-reconciler reversal only, provider-confirmed paid invoices require refund/provider reversal authority | reconciler UI                                                                              |
| `/v1/invoices/:invoiceId/write-off`                  | `POST` | `implemented` | `InvoiceTransitionRequest`           | owner coach / privileged admin / active club owner-admin finance role for linked club invoices | reconciler UI                                                                              |
| `/v1/invoices/:invoiceId/restore`                    | `POST` | `implemented` | `InvoiceTransitionRequest`           | owner coach / privileged admin / active club owner-admin finance role for linked club invoices | reconciler UI                                                                              |
| `/v1/invoices/:invoiceId/void`                       | `POST` | `implemented` | `InvoiceTransitionRequest`           | owner coach / privileged admin / active club owner-admin finance role for linked club invoices | invoice detail + reconciler UI                                                             |
| `/v1/invoices/:invoiceId/reminders`                  | `POST` | `implemented` | `SendInvoiceReminderRequest`         | owner coach / privileged admin / active club owner-admin finance role for linked club invoices | invoice detail send/reminder flow                                                          |
| `/v1/payment-attempts/:attemptId/hosted`             | `GET`  | `implemented` | hosted HTML payment page             | signed attempt token                                                        | external hosted payer payment surface                                                      |
| `/v1/payment-attempts/:attemptId/simulated-complete` | `POST` | `implemented` | `SimulatedPaymentCompleteRequest`    | signed attempt token and stored attempt provider must both be `simulated`; future real-provider attempts cannot complete through this route | backend-owned simulated provider confirmation path                                         |

## Trust / Safeguarding / Ops

| Route                                            | Method            | Status        | Contract(s)                                                            | AuthZ                                                                                                                                                                     | UI anchors                                                                                                                                     |
| ------------------------------------------------ | ----------------- | ------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `/v1/safeguarding/incidents`                     | `POST`            | `implemented` | `CreateSafeguardingIncidentRequest`, `SafeguardingIncidentResponse`    | coach/guardian scoped to athlete/booking context; booking-linked reports create backend `SUPPORT_UPDATE` notifications for the booking coach and club assignment managers | `app/roster/[athleteId]/raise-concern.tsx`, `app/(tabs)/bookings/report-problem.tsx`, `services/trust/index.ts`, `services/concern-service.ts` |
| `/v1/safeguarding/incidents/:incidentId`         | `GET`             | `implemented` | `SafeguardingIncidentResponse`                                         | assignment/role-restricted safeguarding access                                                                                                                            | trust follow-up surfaces, `services/trust/index.ts`                                                                                            |
| `/v1/safeguarding/incidents/:incidentId/actions` | `POST`            | `implemented` | `CreateSafeguardingActionRequest`, `SafeguardingActionResponse`        | safeguarding assignee/admin scope                                                                                                                                         | operations tooling + incident review workflows, `services/trust/index.ts`, `services/concern-service.ts`                                       |
| `/v1/reports`                                    | `GET/POST`        | `implemented` | generic profile/message/review report contracts backed by safeguarding trust records | authenticated reporter; requester is derived from auth, target user must exist, self-report is denied, serious categories auto-block through `/v1/blocks`, and report reads/writes are audited                     | `services/report-service.ts`; live API mode submits and lists the reporter's own profile/message/review reports                                |
| `/v1/blocks`                                     | `GET/POST/DELETE` | `implemented` | block, unblock, blocked-user list, and pairwise block-status contracts | authenticated actor only; requester is derived from auth, self-block is denied, block rows are soft-removed, read/write paths are audited, and pairwise status is server-authoritative for messaging, booking, discovery, and search gates | `services/block-service.ts`; `/v1/users/search` excludes active block relationships in either direction                                       |
| `/v1/access-grants`                              | `GET`             | `implemented` | trust admin overview payload                                           | privileged admin only; success/deny sensitive reads are audited                                                                                                           | ops/admin trust-access surfaces via `apps/api/src/modules/wave2plus/routes.ts`                                                                 |
| `/v1/admin/retention-runs`                       | `GET`             | `implemented` | retention run list payload                                             | privileged admin only; success/deny sensitive reads are audited; DB mode uses Prisma/fixture `RetentionRun` rows instead of seed-store reads                              | ops/admin retention surfaces via `apps/api/src/modules/wave2plus/routes.ts`                                                                    |
| `/v1/me/privacy-settings`                        | `GET`             | `implemented` | self privacy settings payload                                          | authenticated requester self only; sensitive reads are audited                                                                                                            | settings/privacy via `services/privacy-settings-service.ts`                                                                                    |
| `/v1/me/privacy-settings`                        | `PATCH`           | `implemented` | self privacy settings update                                           | authenticated requester self only; changed keys are audited                                                                                                               | settings/privacy via `services/privacy-settings-service.ts`                                                                                    |
| `/v1/me/data-deletion-requests`                  | `GET`             | `implemented` | data-deletion request list payload                                     | authenticated requester self only; success/deny sensitive reads are audited; DB mode uses Prisma/fixture `DataDeletionRequest` rows instead of seed-store reads           | privacy/account surfaces via `apps/api/src/modules/wave2plus/routes.ts`                                                                        |

## Progress / Media / Community / Trust Ops

| Route                                            | Method   | Status        | Contract(s)                                                                                                                            | AuthZ                                                                                                                                                                                                                                       | UI anchors                                                                                                                                                                                                                   |
| ------------------------------------------------ | -------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/v1/athletes/:athleteId/progress`               | `GET`    | `implemented` | db-aware progress/session-note payload                                                                                                 | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | athlete development/progress surfaces via `services/progress-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                      |
| `/v1/athletes/:athleteId/goals`                  | `GET`    | `implemented` | db-aware goals and milestones payload                                                                                                  | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/analytics/analytics-query-service.ts`, `services/progress/progress-goals-service.ts`, goals/development surfaces, and `apps/api/src/modules/wave2plus/routes.ts`                                                   |
| `/v1/athletes/:athleteId/goals`                  | `POST`   | `implemented` | goal create                                                                                                                            | athlete self, guardian, verified assigned coach, or privileged admin through athlete health write gate; writes audit actor and target athlete                                                                                               | `services/progress/progress-goals-service.ts`, compact goal creation, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                         |
| `/v1/goals/:goalId`                              | `GET`    | `implemented` | goal detail with milestones                                                                                                            | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/progress/progress-goals-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                                 |
| `/v1/goals/:goalId`                              | `PATCH`  | `implemented` | goal update/complete/abandon                                                                                                           | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; changed fields are audited                                                                                                    | `services/progress/progress-goals-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                                 |
| `/v1/goals/:goalId/progress`                     | `PATCH`  | `implemented` | manual goal progress override                                                                                                          | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; writes `Goal.progress`, optional milestone completion state, and audit metadata                                               | `services/progress/progress-goals-service.ts`, `services/analytics/analytics-tracking-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                            |
| `/v1/goals/:goalId`                              | `DELETE` | `implemented` | goal soft archive                                                                                                                      | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; archive is audited                                                                                                            | `services/progress/progress-goals-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                                 |
| `/v1/athletes/:athleteId/analytics`              | `GET`    | `implemented` | athlete analytics aggregate from backend session, feedback, skill, goal truth, and population skill averages                           | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/analytics/analytics-query-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                               |
| `/v1/athletes/:athleteId/skills/history`         | `GET`    | `implemented` | athlete skill history from backend skill assessments and definitions                                                                   | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/analytics/analytics-query-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                               |
| `/v1/athletes/:athleteId/practice-logs`          | `GET`    | `implemented` | db-aware self-reported practice logs                                                                                                   | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/progress/progress-practice-log-service.ts`, progress reports/challenges, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                            |
| `/v1/athletes/:athleteId/practice-logs/today`    | `GET`    | `implemented` | current actor's practice log for today                                                                                                 | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/progress/progress-practice-log-service.ts`, progress loop UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                       |
| `/v1/athletes/:athleteId/practice-logs`          | `POST`   | `implemented` | create or accumulate self-reported practice minutes                                                                                    | athlete self, guardian, or scoped coach via athlete health write gate; writes `PracticeLog`, accumulates same-day minutes by actor, and audits success/deny writes                                                                          | `services/progress/progress-practice-log-service.ts`, practice log UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                        |
| `/v1/athletes/:athleteId/progress-challenge`     | `GET`    | `implemented` | active progress challenge state                                                                                                        | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/progress/progress-challenge-service.ts`, progress loop UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                          |
| `/v1/athletes/:athleteId/progress-challenges/history` | `GET` | `implemented` | completed/expired progress challenge history                                                                                           | athlete self, guardian, or scoped coach via athlete health read gate; audited sensitive read                                                                                                                                                | `services/progress/progress-challenge-service.ts`, progress loop UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                          |
| `/v1/athletes/:athleteId/progress-challenges/:challengeId` | `PUT` | `implemented` | create/update/complete/expire one progress challenge state row                                                                         | athlete self, guardian, verified assigned coach, or privileged admin through athlete health write gate; writes `ProgressChallenge`, expires competing active rows, and audits success/deny writes                                           | `services/progress/progress-challenge-service.ts`, progress loop UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                          |
| `/v1/progress-challenges/:challengeId`           | `GET`    | `implemented` | progress challenge detail for id-only completion flows                                                                                 | resolves owning athlete, then applies athlete health read gate; audited sensitive read                                                                                                                                                      | `services/progress/progress-challenge-service.ts`, progress loop UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                          |
| `/v1/athletes/:athleteId/coach-observations`     | `GET`    | `implemented` | coach-authored athlete SEN/strategy observation list backed by tagged `SessionNote` metadata                                            | verified assigned coach or privileged admin through athlete health write scope; reads are audited as sensitive coach notes and guardians are denied                                                                                         | `services/coach-observation-service.ts`, SEN/development observations UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                     |
| `/v1/athletes/:athleteId/coach-observations`     | `POST`   | `implemented` | coach-authored athlete SEN/strategy observation create                                                                                  | verified assigned coach or privileged admin through athlete health write scope; writes tagged `SessionNote` rows and audits success/deny paths                                                                                              | `services/coach-observation-service.ts`, SEN/development observations UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                     |
| `/v1/coach-observations/:observationId`          | `PATCH`  | `implemented` | coach-authored athlete SEN/strategy observation update                                                                                  | original author with verified assigned-coach athlete scope, or privileged admin after resolving the observation; changed fields are audited                                                                                                  | `services/coach-observation-service.ts`, SEN/development observations UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                     |
| `/v1/coach-observations/:observationId`          | `DELETE` | `implemented` | coach-authored athlete SEN/strategy observation soft remove                                                                             | original author with verified assigned-coach athlete scope, or privileged admin after resolving the observation; writes `deletedAt`/`deletedByUserId` and audits `coach_observation.remove`                                                 | `services/coach-observation-service.ts`, SEN/development observations UI, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                     |
| `/v1/athletes/:athleteId/practice-tasks`         | `GET`    | `implemented` | db-aware drill-assignment practice task list with timing state                                                                         | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; success/deny reads are audited                                                                                                         | `services/progress/progress-practice-task-service.ts`, `hooks/use-progress-loop.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/practice-tasks/:taskId/completion`          | `POST`   | `implemented` | practice task completion state backed by assignment submission proof                                                                   | athlete self, linked guardian, or verified assigned coach through athlete health write gate; writes `DrillAssignment` status, creates/retracts `AssignmentSubmission`, and audits success/deny writes                                       | `services/progress/progress-practice-task-service.ts`, `hooks/use-progress-loop.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/practice-tasks/:taskId/due-at`              | `PATCH`  | `implemented` | practice task due date update                                                                                                          | assigned coach with athlete write scope or privileged admin; writes `DrillAssignment.dueDate` and audits success/deny writes                                                                                                                | `services/progress/progress-practice-task-service.ts`, `hooks/use-progress-loop.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/practice-tasks/:taskId/snooze`              | `POST`   | `implemented` | practice task due date snooze                                                                                                          | assigned coach with athlete write scope or privileged admin; writes `DrillAssignment.dueDate` relative to the current due date and audits success/deny writes                                                                               | `services/progress/progress-practice-task-service.ts`, `hooks/use-progress-loop.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/practice-tasks/actions/review`              | `POST`   | `implemented` | bulk practice task review marker                                                                                                       | assigned coach with athlete write scope or privileged admin; writes review `AuditEvent` rows and audits deny paths                                                                                                                          | `services/progress/progress-practice-task-service.ts`, coach follow-up queue surfaces, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                        |
| `/v1/practice-tasks/actions/follow-up`           | `POST`   | `implemented` | bulk practice task follow-up marker                                                                                                    | assigned coach with athlete write scope or privileged admin; writes follow-up `AuditEvent` rows with action type and audits deny paths                                                                                                      | `services/progress/progress-practice-task-service.ts`, coach follow-up queue surfaces, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                        |
| `/v1/practice-tasks/actions/recovery-checkpoint` | `POST`   | `implemented` | bulk recovery checkpoint scheduling                                                                                                    | assigned coach with athlete write scope or privileged admin; writes `DrillAssignment.dueDate`, records recovery-checkpoint `AuditEvent` rows, and audits deny paths                                                                         | `services/progress/progress-practice-task-service.ts`, coach follow-up queue surfaces, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                        |
| `/v1/goals/:goalId/milestones`                   | `POST`   | `implemented` | goal milestone create                                                                                                                  | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; writes audit actor and goal                                                                                                   | `services/progress/progress-goals-service.ts`, `services/analytics/analytics-tracking-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                            |
| `/v1/goals/:goalId/milestones/:milestoneId`      | `PATCH`  | `implemented` | goal milestone update/complete/reopen                                                                                                  | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; writes audit actor, changed fields, and resulting state                                                                       | `services/progress/progress-goals-service.ts`, `services/analytics/analytics-tracking-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                            |
| `/v1/goals/:goalId/milestones/:milestoneId`      | `DELETE` | `implemented` | goal milestone soft archive                                                                                                            | goal owner athlete, guardian, verified assigned coach, or privileged admin through athlete health write gate; archive is audited                                                                                                            | `services/progress/progress-goals-service.ts`, `services/analytics/analytics-tracking-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                            |
| `/v1/milestones/:milestoneId`                    | `PATCH`  | `implemented` | goal milestone update/complete/reopen by global milestone id                                                                           | resolves the owning goal, then applies the same athlete health write gate and audit path as goal-scoped milestone updates                                                                                                                   | legacy milestone-id-only compatibility in `services/progress/progress-goals-service.ts`, goal-aware flows in `services/analytics/analytics-tracking-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`               |
| `/v1/milestones/:milestoneId`                    | `DELETE` | `implemented` | goal milestone archive by global milestone id; returns parent goal payload for legacy service compatibility                             | resolves the owning goal, then applies the same athlete health write gate and audit path as goal-scoped milestone archive                                                                                                                   | legacy milestone-id-only compatibility in `services/progress/progress-goals-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                        |
| `/v1/athletes/:athleteId/session-feedback`       | `GET`    | `implemented` | athlete session feedback list                                                                                                          | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; coach-only feedback is excluded for parent/athlete viewer roles and reads are audited                                                   | `services/progress/progress-feedback-service.ts`, progress dashboards, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                        |
| `/v1/session-feedback`                           | `GET`    | `implemented` | session feedback detail by session id                                                                                                  | same athlete health read gate after resolving the feedback row; coach-only feedback is excluded for parent/athlete viewer roles and reads are audited                                                                                       | booking detail feedback surfaces through `services/progress/progress-feedback-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                      |
| `/v1/session-feedback`                           | `POST`   | `implemented` | session feedback create/update                                                                                                         | assigned coach with athlete write scope or privileged admin; writes rich feedback metadata to `SessionFeedback`, upserts non-coach-only homework into deterministic `Drill`/`DrillAssignment` practice tasks, and audits success/deny paths     | session completion and quick-rate flows through `services/progress/progress-feedback-service.ts`, `hooks/use-session-completion.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                           |
| `/v1/session-media`                              | `GET`    | `implemented` | session media detail by session and athlete                                                                                            | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; reads are audited as sensitive media access                                                                                              | session completion media surfaces through `services/media-service.ts`, `hooks/use-session-media.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                           |
| `/v1/session-media`                              | `PUT`    | `implemented` | session media save for finalized uploaded photos/clips                                                                                 | assigned coach with athlete write scope or privileged admin; requires photo/video consent, only accepts finalized owned `MediaObject` rows, soft-removes omitted prior assets, and audits success/deny paths                                  | session completion media capture through `services/media-service.ts`, `/v1/uploads*`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                         |
| `/v1/session-media/assets/:assetId`              | `DELETE` | `implemented` | session media asset soft remove                                                                                                        | actor must have athlete health write scope for the asset athlete; removal writes `deletedAt` metadata and audits success/deny paths                                                                                                           | session completion media removal through `services/media-service.ts`, `hooks/use-session-media.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                            |
| `/v1/sessions/:sessionId/media`                  | `GET`    | `implemented` | session media list grouped by athlete/session                                                                                          | each returned athlete row must pass athlete health read scope; denied athlete rows are omitted, and the read is audited                                                                                                                       | coach/session media galleries through `services/media-service.ts`, `hooks/use-session-media.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                               |
| `/v1/athletes/:athleteId/session-media`          | `GET`    | `implemented` | athlete session media history                                                                                                          | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; reads are audited as sensitive media access                                                                                              | athlete media history through `services/media-service.ts`, `hooks/use-session-media.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                       |
| `/v1/athletes/:athleteId/skill-updates`          | `POST`   | `implemented` | athlete skill assessment create/update trail                                                                                           | verified assigned coach or privileged admin; writes audit actor, source session/booking, prior score, and resulting score; denied writes are audited                                                                                        | `services/analytics/analytics-tracking-service.ts`, `services/progress/progress-skills-service.ts`, quick-rate/session-completion flows, and `apps/api/src/modules/wave2plus/routes.ts`                                      |
| `/v1/coaches/:coachId/analytics`                 | `GET`    | `implemented` | coach revenue/retention/session metrics from backend bookings, invoices, feedback, and skill assessments                               | coach self or privileged admin; sensitive read is audited, delegated finance scope remains a future finance-only split                                                                                                                      | `components/coach/analytics-screen.tsx`, `services/analytics/analytics-export-service.ts`, `apps/api/src/modules/coach-club/routes.ts`, and `apps/api/src/repositories/p0/coach-analytics-repository.ts`                     |
| `/v1/coaches/:coachId/practice-follow-ups`       | `GET`    | `implemented` | db-aware coach follow-up queue derived from pending drill assignments                                                                  | coach self or privileged admin; success/deny sensitive reads are audited                                                                                                                                                                    | `services/progress/progress-practice-task-service.ts`, `hooks/use-progress-loop.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/athletes/:athleteId/squad-activity`         | `GET`    | `implemented` | db-aware squad peer activity feed aggregate from squad assignment, completed bookings, public feedback, public notes, and badge awards | athlete self, linked guardian, assigned coach through athlete health read gate; success/deny reads are audited, and coach-only/private feedback is excluded                                                                                 | `components/progress/squad-activity-feed.tsx`, `services/progress/progress-squad-activity-service.ts`, and `apps/api/src/modules/wave2plus/routes.ts`                                                                        |
| `/v1/athletes/:athleteId/self-assessments`       | `GET`    | `implemented` | athlete self-assessment list                                                                                                           | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; success/deny reads are audited                                                                                                         | `services/progress/progress-self-assessment-service.ts`, termly progress reports, and `apps/api/src/modules/wave2plus/routes.ts`                                                                                             |
| `/v1/self-assessments`                           | `POST`   | `implemented` | self-assessment submission                                                                                                             | athlete self or linked guardian only; submission must match a completed booking for that athlete, derives coach ownership from the booking, completes the prompt, and audits success/deny writes                                            | self-assessment submission through `services/progress/progress-self-assessment-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                    |
| `/v1/me/self-assessment-prompts`                 | `GET`    | `implemented` | pending self-assessment prompts                                                                                                        | authenticated athlete, linked guardian, assigned coach, or privileged admin through athlete health read gate; prompt rows are derived from backend completed booking proof and success/deny reads are audited                                | pending prompt reads through `services/progress/progress-self-assessment-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                          |
| `/v1/self-assessment-prompts/:promptId/dispatch` | `POST`   | `implemented` | prompt dispatch acknowledgement                                                                                                        | privileged admin only; dispatch state is backend-owned, denied writes are audited, and no client-local dispatch mirror is used                                                                                                              | due prompt dispatch through `services/progress/progress-self-assessment-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                           |
| `/v1/athletes/:athleteId/termly-reports`         | `GET`    | `implemented` | saved termly progress report snapshots                                                                                                | athlete self, linked guardian, assigned coach, or privileged admin through athlete health read gate; sensitive report reads are audited and scoped to visible athlete progress                                                               | `services/progress/progress-termly-report-service.ts`; snapshot history uses backend `TermlyReportSnapshot` rows in API mode                                                                                                  |
| `/v1/athletes/:athleteId/termly-reports`         | `POST`   | `implemented` | save generated termly progress report snapshot                                                                                        | assigned coach, linked guardian, athlete self, or privileged admin only when they can read the generated report; writes audit actor, report range, and target athlete without storing full report content in audit metadata                 | `services/progress/progress-termly-report-service.ts`; snapshot persistence writes backend `TermlyReportSnapshot` rows in API mode                                                                                            |
| `/v1/athletes/:athleteId/weekly-recaps/dispatch` | `POST`   | `implemented` | weekly progress recap notification dispatch                                                                                           | linked guardian recipient and visible athlete progress scope; backend owns week-level dedupe state through durable notification source ids, creates the in-app notification row, and audits send/skip/deny decisions                       | `services/progress/progress-weekly-recap-notification-service.ts`; due API-mode dispatch posts to `/v1` instead of local notification mirrors                                                                                |
| `/v1/athletes/:athleteId/badges`                 | `GET`    | `implemented` | db-aware badges and badge definitions payload                                                                                          | athlete self, guardian, or scoped coach via athlete health read gate; success/deny sensitive reads are audited                                                                                                                              | development proof surfaces via `services/progress-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`                                                                                                                 |
| `/v1/sessions/:sessionId/badges`                 | `GET`    | `implemented` | db-aware session badge award list                                                                                                      | active booking context only; returns awards only for athletes the actor can read through the athlete health read gate; success/deny sensitive reads are audited                                                                             | session badge reads through `services/badge-service.ts` and `apps/api/src/modules/wave2plus/routes.ts`; action state is enriched from backend audit events                                                                    |
| `/v1/athletes/:athleteId/badge-awards`           | `POST`   | `implemented` | badge award create with cooldown override metadata                                                                                     | assigned coach with athlete write scope or privileged admin; actor is derived from auth, optional session linkage must include the target athlete, cooldown overrides require a note, and success/deny writes are audited                    | `services/badge-service.ts`; API mode now creates backend `AthleteBadge` rows through `/v1` instead of local badge storage                                                                                                     |
| `/v1/badge-awards/:awardId/share`                | `POST`   | `implemented` | badge award share marker                                                                                                               | actor must be able to read the linked athlete; coach-only awards can only be shared by assigned coaches or privileged admins; success/deny writes are audited                                                                                | `services/badge-service.ts#markShared`; API mode records backend share state through audited `/v1` action state                                                                                                               |
| `/v1/badge-awards/:awardId/seen`                 | `POST`   | `implemented` | current actor badge seen marker                                                                                                        | actor must be able to read the linked athlete; state is actor-scoped through audit events and returned by badge reads for the current viewer                                                                                                  | `services/badge-service.ts#markSeenByParent`; API mode records backend seen state instead of local badge storage                                                                                                             |
| `/v1/athletes/:athleteId/badge-awards/seen`      | `POST`   | `implemented` | bulk current actor badge seen marker for visible athlete awards                                                                        | actor must be able to read the athlete; coach-only awards are excluded from family bulk seen transitions; each marked award writes an audited backend event                                                                                  | `services/badge-service.ts#markAllSeenByParent`; API mode records backend seen state instead of local badge storage                                                                                                          |
| `/v1/badge-awards/:awardId/feed-post`            | `POST`   | `implemented` | badge award feed-post publish action                                                                                                   | actor must be able to read the linked athlete; coach-only awards can only be posted by assigned coaches or privileged admins; writes real `Post` rows for eligible club targets and audits no-target outcomes                                | `services/badge-service.ts#postBadgeToFeed`; API mode uses backend action state and no longer writes local badge storage                                                                                                     |
| `/v1/drills`                                     | `GET`    | `implemented` | db-aware drill library, assignment, and submission payload; authenticated coaches may omit `coachUserId` to read their own library     | authenticated coach self-read or privileged admin only; success/deny reads are audited and `db` mode reads Prisma `Drill`, `DrillAssignment`, and `AssignmentSubmission` rows                                                               | no retained launch UI anchor; backend authority retained for progress/homework cleanup in `apps/api/src/modules/wave2plus/routes.ts`                                                                                         |
| `/v1/drills/:drillId`                            | `GET`    | `implemented` | db-aware drill detail with assignment and submission payload                                                                           | owning coach or privileged admin only; success/deny reads are audited as `drill.read` and `db` mode reads Prisma `Drill`, active `DrillAssignment`, and `AssignmentSubmission` rows                                                         | `services/drill-service.ts#getDrillById`; API mode reads detail through `/v1` instead of returning null                                                                                                                       |
| `/v1/drills`                                     | `POST`   | `implemented` | db-aware drill library create; UI-only drill fields are stored in `metadataJson` and flattened on response                             | authenticated coach self or privileged admin; writes audit actor, drill metadata, and initial visibility as `drill.create`                                                                                                                   | `services/drill-service.ts#createDrill`; API mode creates through `/v1` instead of local storage                                                                                                                              |
| `/v1/drills/:drillId`                            | `PATCH`  | `implemented` | db-aware drill library update; updates title, description, difficulty, and flattened metadata-backed UI fields                          | owning coach or privileged admin; writes changed fields and audits allow/deny paths as `drill.update`                                                                                                                                        | `services/drill-service.ts#updateDrill`; API mode updates through `/v1` instead of local storage                                                                                                                              |
| `/v1/drills/:drillId`                            | `DELETE` | `implemented` | db-aware drill library soft remove; removal sets `active=false` and `deletedAt` without deleting assignments/submissions               | owning coach or privileged admin; soft-removes drill and audits allow/deny paths as `drill.remove`                                                                                                                                           | `services/drill-service.ts#deleteDrill`; API mode removes through `/v1` instead of local storage                                                                                                                              |
| `/v1/drill-assignments`                          | `POST`   | `implemented` | assign drill to athlete                                                                                                                | assigned coach with athlete write scope or privileged admin; creates `DrillAssignment`, blocks parent/athlete assignment writes, restricts private drills to author/admin, and audits success/deny writes                                  | direct `services/drill-service.ts` assignment create through `/v1`; stores drill, athlete, due date, instructions, evidence requirement, status, and audit metadata                                                           |
| `/v1/athletes/:athleteId/drill-assignments`      | `GET`    | `implemented` | db-aware athlete drill assignment list with drill and submission context                                                                | athlete self, guardian, or scoped coach through athlete health read gate; success/deny sensitive reads are audited                                                                                                                          | direct `services/drill-service.ts` assignment reads through `/v1`; practice-task reads use `/v1/athletes/:athleteId/practice-tasks`                                                                                          |
| `/v1/drill-assignments/:assignmentId/completion` | `PATCH`  | `implemented` | drill assignment completion toggle with optional text evidence                                                                         | athlete self, linked guardian, assigned coach, or privileged admin through athlete health write gate; writes completion state and audit metadata; video evidence still requires upload/media proof before completion                       | direct `services/drill-service.ts` completion and undo use this route in API mode; practice-task completion uses `/v1/practice-tasks/:taskId/completion`                                                                     |
| `/v1/drill-assignments/:assignmentId`            | `DELETE` | `implemented` | drill assignment soft remove                                                                                                           | assigned coach with athlete write scope or privileged admin only; sets `deletedAt`, keeps submission proof, and audits success/deny/error paths as `drill_assignment.remove`                                                               | direct `services/drill-service.ts` removal uses this route in API mode; normal assignment and practice-task reads exclude soft-removed rows                                                                                   |
| `/v1/uploads/init`                               | `POST`   | `implemented` | scaffold upload-init payload                                                                                                           | authenticated uploader; db mode requires signed private object-storage config                                                                                                                                                               | media/video/verification upload entrypoints via `apps/api/src/modules/wave2plus/routes.ts`; db mode persists pending `MediaObject` and `UploadSession`, seed mode keeps the placeholder URL                                  |
| `/v1/uploads/:uploadSessionId/complete`          | `POST`   | `implemented` | scaffold upload-complete payload                                                                                                       | authenticated upload requester only; requires an existing latest `CLEAN` malware-scan result before media becomes available; missing, pending, or unsafe scan proof leaves the media unusable                                                | video upload flow via `services/video-service.ts`; denied outsider completion and missing scan proof are audited/blocked before video creation                                                                               |
| `/v1/videos`                                     | `GET`    | `implemented` | scaffold video list payload                                                                                                            | authenticated actor; `coachId` and `athleteId` filters fail closed through video authority checks                                                                                                                                           | coach library and athlete video tabs via `services/video-service.ts`                                                                                                                                                         |
| `/v1/videos`                                     | `POST`   | `implemented` | scaffold video create payload                                                                                                          | authenticated coach/admin with owned media that is already finalized and has latest clean malware-scan proof                                                                                                                                | video upload flow via `app/videos/upload.tsx`, `hooks/use-video-upload.ts`, `services/video-service.ts`; pending, quarantined, or unscanned media is rejected and audited                                                    |
| `/v1/videos/:videoId`                            | `GET`    | `implemented` | scaffold video detail payload                                                                                                          | privileged admin, delivery coach, athlete self, or explicitly shared linked guardian                                                                                                                                                        | `hooks/use-video-detail.ts`, `components/video/*`, `services/video-service.ts`                                                                                                                                               |
| `/v1/videos/:videoId`                            | `PATCH`  | `implemented` | scaffold video update payload                                                                                                          | privileged admin or owning coach                                                                                                                                                                                                            | video detail edit surfaces via `services/video-service.ts`                                                                                                                                                                   |
| `/v1/videos/:videoId`                            | `DELETE` | `implemented` | no body; soft archive                                                                                                                  | privileged admin or owning coach; writes `deletedAt` and audits the archive action                                                                                                                                                          | video archive/remove actions via `services/video-service.ts`                                                                                                                                                                  |
| `/v1/videos/:videoId/share`                      | `PATCH`  | `implemented` | scaffold video share payload                                                                                                           | privileged admin or owning coach; recipient scope is fail-closed to linked athlete/guardian users                                                                                                                                           | video share/private actions via `hooks/use-video-detail.ts`, `components/video/*`, `services/video-service.ts`                                                                                                               |
| `/v1/videos/:videoId/annotations`                | `POST`   | `implemented` | scaffold video annotation payload                                                                                                      | privileged admin or owning coach                                                                                                                                                                                                            | annotation create flows via `services/video-service.ts`, `components/video/*`                                                                                                                                                |
| `/v1/videos/:videoId/annotations/:annotationId`  | `PATCH`  | `implemented` | scaffold video annotation payload                                                                                                      | privileged admin or owning coach                                                                                                                                                                                                            | annotation edit flows via `services/video-service.ts`, `components/video/*`                                                                                                                                                  |
| `/v1/videos/:videoId/annotations/:annotationId`  | `DELETE` | `implemented` | no body; soft archive                                                                                                                  | privileged admin or owning coach; writes `deletedAt` and audits the archive action                                                                                                                                                          | annotation archive/remove flows via `services/video-service.ts`, `components/video/*`                                                                                                                                        |
| `/v1/community-groups`                           | `GET`    | `implemented` | scaffold community-group list                                                                                                          | authenticated member of each returned group                                                                                                                                                                                                 | community group hubs and parent chat surfaces; current backend owner is `apps/api/src/repositories/p0/community-media-repository.ts`                                                                                         |
| `/v1/community-groups`                           | `POST`   | `implemented` | scaffold community-group create for `GENERAL`, `CLUB`, and private `SQUAD` groups                                                      | authenticated actor creates `GENERAL` groups as owner; `CLUB` groups require active club staff or privileged admin; `SQUAD` groups require `squadId`, derive `clubId` from the squad, stay private, are idempotent by one active group per squad, and can only seed users assigned to the squad as owner coach, linked athlete user, or linked guardian; idempotency replay/conflict is enforced and writes are audited | community group creation via `services/community/community-group-service.ts`, `services/community-media-authority-service.ts`, and `services/squad-group-service.ts`; session group creation remains fail-closed until dedicated session membership authority exists |
| `/v1/community-groups/:groupId/join`             | `POST`   | `implemented` | no body                                                                                                                                | authenticated actor can join public groups only; club-scoped groups additionally require active club membership, squad groups additionally require squad assignment, repeat joins conflict, and allowed/denied attempts are audited                                                              | public community group join flow via `services/community/community-group-service.ts` and `services/community-media-authority-service.ts`; private/coach approval flows use the join-request routes below                    |
| `/v1/community-groups/:groupId/join-requests`    | `POST`   | `implemented` | optional `isCoach` boolean                                                                                                             | authenticated actor can request membership for themselves only; active members and duplicate pending requests conflict, club-scoped group requests require active club membership, squad group requests require squad assignment, and allowed/denied attempts are audited                    | coach/private community group request flow via `services/community/community-group-service.ts` and `services/community-media-authority-service.ts`                                                                             |
| `/v1/community-groups/:groupId/join-requests`    | `GET`    | `implemented` | no body                                                                                                                                | authenticated group owner/admin or privileged admin only; reads pending requests for that group and writes a sensitive-read audit event                                                                                                        | group owner/admin approval queue via `services/community/community-group-service.ts`                                                                                                                                          |
| `/v1/community-groups/:groupId/join-requests/:requestId/approve` | `POST` | `implemented` | no body                                                                                                               | authenticated group owner/admin or privileged admin only; approving creates/reactivates a `MEMBER` membership for the requester, re-checks squad assignment for squad groups, marks the request accepted, notifies the requester, and audits allow/deny paths                            | community group join approval via `services/community/community-group-service.ts`                                                                                                      |
| `/v1/community-groups/:groupId/join-requests/:requestId/reject` | `POST` | `implemented` | no body                                                                                                                | authenticated group owner/admin or privileged admin only; rejecting marks the request declined without granting membership, notifies the requester, and audits allow/deny paths                                                               | community group join rejection via `services/community/community-group-service.ts`                                                                                                                                             |
| `/v1/community-groups/:groupId/leave`            | `POST`   | `implemented` | no body                                                                                                                                | authenticated active group member only; membership is soft-removed with `active=false`/`deletedAt`, sole owner/admin cannot leave while other members remain, and allowed/denied attempts are audited as `community.group.leave`             | community group leave flow via `services/community/community-group-service.ts`; avoids hard-delete wording/behavior for membership removal                                                                                    |
| `/v1/community-groups/:groupId/members`          | `POST`   | `implemented` | `memberUserId` plus optional role enum: `ADMIN`, `MODERATOR`, or `MEMBER`                                                               | authenticated group owner/admin or privileged admin only; requester is derived from auth, target user must exist, active duplicates are no-op success, club groups require target club membership unless privileged admin, squad groups require target squad assignment, non-owner admins cannot assign equal/higher roles, and allowed/denied attempts are audited | direct community group member add via `services/community/community-group-service.ts` and `services/community-media-authority-service.ts`; `OWNER` assignment uses transfer ownership instead                                 |
| `/v1/community-groups/:groupId/members/:memberUserId/role` | `PATCH` | `implemented` | role enum: `ADMIN`, `MODERATOR`, or `MEMBER`                                                                                           | authenticated group owner/admin or privileged admin only; requester is derived from auth, self role changes and owner assignment are denied, sole owner/admin demotion is blocked, and allowed/denied attempts are audited                  | community group role management via `services/community/community-group-service.ts`; `OWNER` changes use the transfer-ownership route                                                                                         |
| `/v1/community-groups/:groupId/members/:memberUserId/transfer-ownership` | `POST` | `implemented` | no body                                                                                                               | authenticated current group owner or privileged admin only; target must be an active member, target becomes `OWNER`, previous owner memberships are demoted to `ADMIN`, and allowed/denied attempts are audited                            | community group owner transfer via `services/community/community-group-service.ts`                                                                                                      |
| `/v1/community-groups/:groupId/members/:memberUserId/remove` | `POST` | `implemented` | no body                                                                                                                                | authenticated group owner/admin or privileged admin only; requester is derived from auth, membership is soft-removed, self removal must use leave, orphaning the only owner/admin is blocked, and allowed/denied attempts are audited         | community group member removal via `services/community/community-group-service.ts`; uses removal/archive wording instead of hard delete                                                                                       |
| `/v1/community-groups/:groupId/archive`          | `POST`   | `implemented` | no body                                                                                                                                | authenticated group owner or privileged admin only; requester is derived from auth, the group and active memberships are soft-archived, archived groups disappear from normal reads, and allowed/denied attempts are audited as `community.group.archive` | legacy `deleteGroup()` compatibility in `services/community/community-group-service.ts`; product copy should say archive/remove rather than hard delete                                                                      |
| `/v1/community-groups/:groupId/invites`          | `POST`   | `implemented` | invitee user id plus optional message                                                                                                   | authenticated group owner/admin or privileged admin only; target user must exist, active members and duplicate pending invites conflict, club group invitees must be active club members, squad group invitees must be squad-assigned, and allowed/denied attempts are audited             | community group invite create via `services/community/community-group-service.ts`                                                                                                                                             |
| `/v1/me/community-group-invites`                 | `GET`    | `implemented` | no body                                                                                                                                | authenticated user reads their pending active community group invite targets only                                                                                                                                                           | community group invite inbox via `services/community/community-group-service.ts`                                                                                                                                              |
| `/v1/community-group-invites/:inviteId/accept`   | `POST`   | `implemented` | no body                                                                                                                                | authenticated invite target only; accepting creates/reactivates a `MEMBER` group membership, re-checks squad assignment for squad groups, marks invite target accepted, and audits allow/deny paths                                                                                       | community group invite accept via `services/community/community-group-service.ts`                                                                                                                                            |
| `/v1/community-group-invites/:inviteId/decline`  | `POST`   | `implemented` | no body                                                                                                                                | authenticated invite target only; declining marks invite target declined without granting access and audits allow/deny paths                                                                                                                | community group invite decline via `services/community/community-group-service.ts`                                                                                                                                           |
| `/v1/follows`                                    | `GET/POST/PATCH/DELETE` | `implemented` | follow, unfollow, notification preferences, follower/following list, and follow status contracts                                                         | authenticated actor only; actor comes from auth, target users must exist, active blocks in either direction deny follow access, following-list reads are self/admin only, follower-list reads require no block relationship, preference updates only touch the actor's active follow row, mutations are soft-remove/idempotent, and allowed/denied paths are audited | `services/follow-service.ts`; API mode uses server-authoritative `UserFollow` rows for profile connection state, feed filtering, and follow notification preferences instead of local follow storage                                                                        |
| `/v1/follow-requests`                            | `GET/POST` | `implemented` | private follow request inbox and create contracts                                                         | authenticated requester/target only; users can read their own incoming requests or their own outgoing request to a target, duplicate pending requests return the existing request, block relationships deny access, and all reads/creates are audited                                                                              | `services/follow-service.ts`; API mode uses server-authoritative `UserFollowRequest` rows for private-profile connection state instead of local request storage                                                                        |
| `/v1/follow-requests/:requestId`                 | `PATCH` | `implemented` | accept or decline a private follow request                                                         | authenticated target only; accept creates mutual `UserFollow` rows, decline does not grant access, repeated terminal responses are denied by backend state, block relationships deny access, and allowed/denied decisions are audited                                                                              | `services/follow-service.ts`; API mode responds to follow requests through server-authoritative `UserFollowRequest` rows instead of local request storage                                                                        |
| `/v1/posts`                                      | `GET`    | `implemented` | scaffold post list payload                                                                                                             | authenticated author, readable club member, or readable group member; `clubId` and `communityGroupId` filters fail closed for outsiders                                                                                                     | club/community feeds and post surfaces, including homepage club highlights through `services/social-feed-service.ts#getFeedAuthority`; current backend owner is `apps/api/src/repositories/p0/community-media-repository.ts` |
| `/v1/posts`                                      | `POST`   | `implemented` | scaffold staff post create payload                                                                                                     | authenticated active club/group staff or privileged admin only; author is derived from auth, media attachments are denied until backend upload proof exists, idempotency replay/conflict is enforced, and allowed/denied writes are audited | club/staff post composer via `services/social-feed-service.ts`; API mode fails closed instead of using local feed state for top-level post creation                                                                          |
| `/v1/posts/:postId/reactions/toggle`             | `POST`   | `implemented` | scaffold post reaction payload                                                                                                         | authenticated actor must be able to read the target post through author, group, club, or privileged-admin scope                                                                                                                             | club feed like action through `services/social-feed-service.ts#toggleReactionAuthority`; actor comes from auth and allowed/denied toggles are audited                                                                        |
| `/v1/posts/:postId/comments`                     | `GET`    | `implemented` | scaffold post-comment list                                                                                                             | authenticated actor must be able to read the target post through author, group, club, or privileged-admin scope                                                                                                                             | post detail comments via `services/comment-service.ts`; soft-deleted comments remain visible as deleted placeholders to preserve thread shape                                                                                |
| `/v1/posts/:postId/comments`                     | `POST`   | `implemented` | scaffold post-comment create                                                                                                           | authenticated readable-post actor only; author is derived from auth, one-level replies are enforced, idempotency replay/conflict is enforced, and allowed/denied writes are audited                                                         | post detail comment/reply submission via `services/comment-service.ts`; API mode ignores client-supplied author ids                                                                                                          |
| `/v1/comments/:commentId`                        | `GET`    | `implemented` | scaffold post-comment detail                                                                                                           | authenticated actor must be able to read the comment's target post                                                                                                                                                                          | comment detail compatibility via `services/comment-service.ts`                                                                                                                                                               |
| `/v1/comments/:commentId`                        | `DELETE` | `implemented` | no body                                                                                                                                | authenticated comment author or privileged admin only; soft remove is audited as `community.comment.remove` and repeat remove conflicts                                                                                                     | post detail remove action via `services/comment-service.ts`                                                                                                                                                                  |
| `/v1/comments/:commentId/reactions/toggle`       | `POST`   | `implemented` | no body                                                                                                                                | authenticated readable-post actor only; actor comes from auth, deleted comments reject reactions, and allowed/denied toggles are audited                                                                                                    | post detail comment like action via `services/comment-service.ts`; API mode no longer writes local comment-like state                                                                                                        |
| `/v1/community-groups/:groupId/messages`         | `POST`   | `implemented` | scaffold group message create                                                                                                          | authenticated active group member only; sender is derived from auth, attachments are denied until backend media proof exists, idempotency replay/conflict is enforced, and allowed/denied writes are audited                                | group chat send flow via `services/community/community-messaging-service.ts` and `services/community-media-authority-service.ts`                                                                                             |
| `/v1/community-groups/:groupId/messages/read`    | `POST`   | `implemented` | scaffold group message read receipt                                                                                                    | authenticated active group member only; read receipts and participant `lastReadAt` are backend-owned and allowed/denied writes are audited                                                                                                  | group chat read state via `services/community/community-messaging-service.ts` and `services/community-media-authority-service.ts`                                                                                            |
| `/v1/message-threads/:threadId/messages`         | `POST`   | `implemented` | scaffold direct/thread message create                                                                                                  | authenticated active thread participant only; sender is derived from auth, attachments are denied until backend media proof exists, idempotency replay/conflict is enforced, and allowed/denied writes are audited                          | direct chat send flow via `services/messaging-service.ts` and `services/community-media-authority-service.ts`; API mode no longer creates local-only direct messages                                                         |
| `/v1/message-threads/:threadId/read`             | `POST`   | `implemented` | direct/thread message read receipt                                                                                                     | authenticated active thread participant only; read receipts and participant `lastReadAt` are backend-owned and allowed/denied writes are audited                                                                                            | direct chat mark-read through `services/messaging-service.ts` and `services/community-media-authority-service.ts`                                                                                                             |
| `/v1/messages/:messageId`                        | `DELETE` | `implemented` | no body                                                                                                                                | authenticated message sender or privileged admin only; soft remove is audited as `community.message.remove` and repeat remove conflicts                                                                                                     | direct/group message remove action via `services/messaging-service.ts`; API mode no longer hides messages with local-only remove overlays                                                                                    |
| `/v1/message-threads`                            | `GET`    | `implemented` | scaffold message-thread list                                                                                                           | authenticated participant only                                                                                                                                                                                                              | chat/message surfaces; current backend owner is `apps/api/src/repositories/p0/community-media-repository.ts`                                                                                                                 |
| `/v1/me/notifications`                           | `GET`    | `implemented` | scaffold notification list                                                                                                             | authenticated self                                                                                                                                                                                                                          | notifications inbox/toasts/preferences surfaces; current backend owner is `apps/api/src/repositories/p0/community-media-repository.ts`                                                                                       |
| `/v1/me/notifications/preferences`               | `PATCH`  | `implemented` | scaffold notification preference update                                                                                                | authenticated self only; backend owns channel, quiet-hours, type-preference, and muted-coach state, ignores forged user ownership, and audits updates                                                                                       | notification settings via `services/notification/notification-preferences.ts` and `services/community-media-authority-service.ts`                                                                                            |
| `/v1/me/notifications/read-all`                  | `POST`   | `implemented` | scaffold bulk read receipt                                                                                                             | authenticated self only; backend marks visible notifications read and audits the mutation                                                                                                                                                   | notifications inbox read-all via `services/notification/notification-store.ts` and `services/community-media-authority-service.ts`                                                                                           |
| `/v1/me/notifications/dismiss-all`               | `POST`   | `implemented` | scaffold bulk dismiss                                                                                                                  | authenticated self only; backend dismisses visible notifications and audits the mutation                                                                                                                                                    | notifications inbox clear-all via `services/notification/notification-store.ts` and `services/community-media-authority-service.ts`                                                                                          |
| `/v1/me/notifications/:notificationId/read`      | `POST`   | `implemented` | scaffold notification read receipt                                                                                                     | authenticated owner only; denied cross-user mutations are audited                                                                                                                                                                           | notification read actions via `services/notification/notification-store.ts` and `services/community-media-authority-service.ts`                                                                                              |
| `/v1/me/notifications/:notificationId/dismiss`   | `POST`   | `implemented` | scaffold notification dismiss                                                                                                          | authenticated owner only; denied cross-user mutations are audited                                                                                                                                                                           | notification dismiss actions via `services/notification/notification-store.ts` and `services/community-media-authority-service.ts`                                                                                           |

Keep this file as the core `/v1` inventory index.
When these modules expand, add route rows here and update:

- `docs/backend-api/AUTHZ_AUDIT_AND_SECURITY.md`
- `docs/backend-api/API_CONTRACTS_ERRORS_AND_HANDLERS.md`
- `docs/architecture/service-ownership-map.md` when frontend service ownership changes

## Inventory Rules

- Every row must eventually include:
  - shared contract name(s)
  - authz rule summary (role + grants + safety gates)
  - idempotency requirement (for writes)
  - versioning/conflict requirement (for mutable resources)
  - UI route/service anchors
- `npm run api:openapi` regenerates the checked-in OpenAPI document from `implemented` and `scaffolded` rows in this inventory.
- Route-specific OpenAPI overrides live in `scripts/generate-openapi-from-route-inventory.js` when a high-value route needs explicit tags, query parameters, or request/response schemas beyond the inventory row summary.
- OpenAPI/Swagger routes are enabled by default outside production. Production must opt in with `API_OPENAPI_ENABLED=1`.
- HTTP `DELETE` remains valid for resource removal endpoints, but human-facing product copy and audit-log rendering must describe the real effect: `archive`, `remove`, `dismiss`, `revoke`, `cancel`, or `void`. For example, render `athlete.remove` as “Athlete removed” rather than showing raw action IDs to users.
