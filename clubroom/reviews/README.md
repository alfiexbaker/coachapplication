# Review Workspace

Purpose: transient review output only.

Use dated subfolders such as:

- `codebase-audit/2026-04-01/`

Rules:

- Do not treat files here as canonical product or implementation docs.
- If a finding is accepted, move only the minimal truth into the correct canonical doc.
- The live execution queue remains `docs/newsprints/sprints/BACKLOG.md` plus `docs/newsprints/sprints/laststep.md`.
