# Feature Status Matrix

Date: 2026-04-01

| Area | Role(s) | Status | Evidence | Blocking Gap | Recommended Next Action |
| --- | --- | --- | --- | --- | --- |
| Dev bearer auth and session lifecycle | All signed-in roles | `WORKING` | `/v1/auth/login`, `/v1/auth/refresh`, `/v1/auth/logout`, `/v1/auth/revoke`, `/v1/auth/me` in `apps/api/src/modules/auth/routes.ts`; `/v1/me/sessions*` in `apps/api/src/modules/identity/routes.ts`; verified by `apps/api/src/modules/auth/routes.test.ts`, `apps/api/src/modules/p0-core/routes.test.ts`, `npm --prefix apps/api run test` | Still depends on temporary auth plugin for request auth context | Keep the route surface, replace the plugin underneath it |
| Launch-safe production auth | All signed-in roles | `SCAFFOLDED` | `apps/api/src/plugins/auth-placeholder.ts` accepts scaffold headers and defaults to `usr_dev-scaffold`; `docs/newsprints/sprints/BACKLOG.md` marks `AUTH-02` as `NOW` | No production-grade identity validation or default-deny runtime | Replace scaffold fallback with real JWT/session validation and broader backend authz integration |
| Frontend auth service path | All signed-in roles | `PARTIAL` | `services/auth-service.ts` handles token storage, refresh, and `/v1/auth/me`; `hooks/use-auth.tsx` still contains large mock/demo logic | Frontend auth still mixes real backend calls with demo-user and mock-state branches | Shrink `use-auth` to one backend-authoritative path and isolate mock-only adapters |
| Invite CRUD and response authority | Coach, parent, club admin | `WORKING` | `/v1/invites` create/list/detail/delete/remind/dismiss/respond in `apps/api/src/modules/booking/routes.ts`; frontend path in `services/invite/session-invite-authority-service.ts`, `services/invite/session-invite-service.ts`, `app/session-invites/index.tsx`, `app/session-invites/[id].tsx`; verified by `apps/api/src/modules/p0-core/routes.test.ts` | None inside the audited `/v1` invite operations | Keep invite reads/writes on `/v1` and use this seam as the booking-authority baseline |
| Booking creation authority across all flows | Coach, parent, club admin | `PARTIAL` | `apps/api/src/modules/booking/routes.ts` enforces `bookedByUserId === authUserId` unless club admin; `services/booking/booking-crud-service.ts` logs `Skipping API booking create for delegated actor` and falls back locally | Delegated create is still not backend-authoritative | Add an explicit delegated-booking authz contract and remove the local-only fallback |
| Family-health authz on `/v1` | Guardian, verified coach | `PARTIAL` | `/v1/athletes/:athleteId/injuries`, `/medical`, `/emergency-contacts`, `/consents` in `apps/api/src/modules/family-athlete/routes.ts`; frontend path in `services/family/family-health-service.ts`; verified by `apps/api/src/modules/family-athlete/routes.test.ts`; current client access still builds `x-auth-*` headers and depends on `apps/api/src/plugins/auth-placeholder.ts` | Route coverage exists, but launch auth still depends on scaffold/header auth and seed-backed persistence | Keep the route shape, finish `AUTH-02`, then treat family-health as backend-authoritative |
| Child profile trust-sensitive coherence | Guardian, child | `BROKEN` | `services/child-service.ts` still reads/writes `/api/children` and stores allergies, medical conditions, and emergency contacts; `app/(modal)/edit-child-profile.tsx` edits those fields directly; `hooks/use-child-context.tsx` reconciles multiple child sources | Two systems can own the same medical/emergency truth | Remove trust-sensitive fields from `child-service` or make it proxy `/v1` family-health |
| Progress and analytics surfaces | Coach, parent, athlete | `SCAFFOLDED` | `services/progress/progress-feedback-service.ts` and `services/progress/progress-goals-service.ts` persist via `apiClient` storage; `services/analytics/analytics-query-service.ts`, `services/analytics/analytics-export-service.ts`, and `services/analytics/analytics-tracking-service.ts` still reference legacy `/api/athletes/*` and `/api/goals/*`; `hooks/use-my-progress.ts` reads local stores | Frontend progress does not use the backend-authoritative routes that now exist in the API test suite | Decide the minimal launch progress slice and wire it to `/v1` only |
| Club schedule surface | Club staff, parents, athletes | `PARTIAL` | `services/club-schedule-service.ts` composes `eventService.getAllClubEvents`, `loadSessions()`, and `matchService.getClubMatches`; backlog keeps `LAUNCH-01` open in `docs/newsprints/sprints/BACKLOG.md` | No single backend-authoritative schedule surface | Add a `/v1` schedule read model and move the screen to it |
| Event workspace | Club staff, attendees | `BROKEN` | `hooks/use-event-detail.ts` supports RSVP, reminders, check-in, attendance, and recap handoff; `app/events/[id].tsx` renders the workspace; in non-mock mode `services/event/event-rsvp-service.ts` and `services/event/event-attendance-service.ts` still call relative legacy `/api/events/*` URLs instead of `/v1`; backlog keeps `LAUNCH-02` open | The rich UI exists, but the real non-mock authority path is not wired to the current API contract | Build `/v1` RSVP and attendance routes/services first, then harden recap publishing on top of them |
| Coach storefront and booking entry | Parents, athletes, coaches | `PARTIAL` | Public coach detail and booking CTA exist in `app/coach/[id].tsx` and `hooks/use-coach-detail.ts`; booking entry routes through `Routes.bookCoach`; `hooks/use-coach-profile.ts` still reads `session_offerings` from local storage and simulates go-live with `setTimeout` | Conversion UX exists, but source data is still local/mock heavy | Keep the UX, move offerings and go-live state to backend authority |
| Club profile/storefront | Parents, athletes, club staff | `PARTIAL` | `app/club/[id].tsx` and `hooks/use-club-detail.ts` render stats, membership, posts, and sharing; data is sourced from `socialFeedService` and `clubService` app-side stores | Surface is more member ops hub than authoritative public storefront | Decide whether launch needs public conversion or member ops first, then align the data model |
| Reviews and proof | Parents, athletes | `SCAFFOLDED` | `services/review-service.ts` sets `isVerifiedBooking` from `!!bookingId`; `services/review-sync-service.ts` mirrors that same assumption; `services/coach-service.ts` seeds `MOCK_REVIEWS`; backlog keeps `LAUNCH-03` open | “Verified” proof is local-state derived, not backend-validated | Make verified reviews depend on real booking rows from the backend |
| Rebook loop | Parents, athletes | `PARTIAL` | `app/(tabs)/bookings/[id].tsx` shows review and rebook CTAs; `hooks/use-booking-detail.ts` rebook routes to `Routes.bookSchedule(booking.coachId)` | Rebook exists as navigation only, not as a tightened conversion loop with authoritative offerings | Pair rebook with authoritative offerings and coach availability |
| Role landing and primary home | Parent, coach, user, admin | `PARTIAL` | `app/(tabs)/index.tsx` routes parent to `FamilyCalendarScreen`, coach to `CoachDevelopmentScreen`, user to `UserHomeScreen`, admin to `AdminUsersScreen` | Parent entry is coherent; coach launch entry still biases development over bookings/schedule | Pick the launch-primary coach home and remove ambiguity |
| Updates feed | Parent, coach, member | `PARTIAL` | `app/(tabs)/feed.tsx` uses `socialFeedService` and `followService`; empty state is explicit about clubs, follows, and bookings | Feed is usable, but still app-owned and relationship-derived from local stores | Keep the honest UX and back it with authoritative follow/membership state only if it survives launch scope |
| Observability / Sentry | Engineering, ops | `BROKEN` | Only config references found in `app.config.ts` and `constants/config.ts`; backlog keeps `OBS-01` open in `docs/newsprints/sprints/BACKLOG.md`; no meaningful SDK usage found by repo search | Production errors are not traceable end-to-end | Wire Sentry across Expo native, Expo web, and `apps/api` before broad launch testing |
| Repo-critical validation scripts | Engineering | `PARTIAL` | `package.json` and `apps/api/package.json` expose typecheck, compile, tests, architecture audit; all passed on 2026-04-01 via `npm run typecheck`, `npm run test:compile`, `npm --prefix apps/api run typecheck`, `npm --prefix apps/api run test`, `npm run audit:architecture` | Passing checks still do not prove launch safety because they do not cover end-to-end frontend authority seams or production auth | Keep the checks, but add E2E coverage only after the authority seams are closed |
