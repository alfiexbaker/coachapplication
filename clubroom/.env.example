# Clubroom Environment Configuration

# Mock mode toggle — set to 'false' to use real API
EXPO_PUBLIC_USE_MOCK=true

# API base URL (only used when USE_MOCK=false)
EXPO_PUBLIC_API_URL=http://localhost:3000
